# Achmage Slides Ultra 작업 규칙

이 파일은 저장소 전체에 적용된다. 기능, 버그 수정, 리팩터링, 문서, 빌드, 배포 설정을 업데이트할 때는 아래의 **Research → Plan → Execution → Verification** 흐름을 따른다.

## 세션 시작 시 읽기 순서

1. `docs/research/REGISTER.md`에서 관련 주제와 상태를 먼저 찾는다.
2. 이번 작업과 직접 관련된 연구 보고서만 전문을 읽는다.
3. 구현 요청이면 연결된 `docs/plans/P-NNN.md`와 `docs/execution/E-NNN.md`를 읽는다.
4. 인덱스에 관련 연구가 없거나 보고서가 오래되었으면 구현보다 연구를 먼저 한다.

모든 보고서를 무조건 읽지 않는다. Register는 라우팅용이고, 전체 보고서는 관련성이 확인된 것만 읽는다.

## 필수 게이트

- **Research gate:** 조사 요청은 반드시 `docs/research/reports/R-NNN.md`에 축적한다. 채팅 답변만 남기고 끝내지 않는다.
- **Plan gate:** 저장소 업데이트 계획은 하나 이상의 `complete` 상태 R-ID를 참조해야 한다. 보고서의 ID만 장식적으로 적지 말고, Finding ID(`F-NNN`)를 계획 결정과 검증 항목에 연결한다.
- **Execution gate:** 제품 또는 운영 파일을 수정하기 전에 승인된 계획과 실행 기록이 있어야 한다. 실행 기록은 계획이 인용한 모든 R-ID를 그대로 계승한다.
- **Verification gate:** 완료 선언 전에 실행 기록에 실제 명령, 결과, 변경 경로, 계획 이탈, 남은 위험을 기록하고 `npm run governance:check`를 통과시킨다.
- 연구 근거와 충돌하는 구현이 필요하면 조용히 우회하지 않는다. 연구 보고서와 계획을 먼저 개정하고 변경 이유를 남긴다.
- 생성된 R/P/E 기록은 append-only다. 완료되거나 대체된 기록도 삭제하거나 ID·생성일·주제 소유권을 재사용하지 않고 상태와 관계로 보존한다.

명시적인 구현 요청 자체가 계획 실행 승인이 될 수 있다. 다만 연구와 계획 문서가 없다는 이유로 이 게이트를 생략할 수는 없다.

## R-ID를 새로 만들지 판단하는 규칙

다음 세 조건이 모두 같으면 기존 보고서를 갱신한다.

1. 해결하려는 핵심 의사결정이 같다.
2. 주된 조사 질문과 성공 기준이 같다.
3. 영향을 받는 제품 표면과 범위가 실질적으로 같다.

근거 보강, 최신 정보 재확인, 같은 가설의 추가 실험은 같은 R-ID에 추가하고 Revision log를 갱신한다. 반대로 핵심 질문, 독립적으로 구현 가능한 결과, 주요 하위 시스템, 성공 기준 중 하나라도 실질적으로 달라지면 새 R-ID를 만든다. 애매하면 새 ID를 만들고 `related`로 연결한다. 기존 ID는 삭제하거나 다른 주제에 재사용하지 않는다.

## 연구 품질 기준

완료된 보고서는 최소한 다음을 충족해야 한다.

- 질문, 배경, 범위, 비범위, 성공 기준을 명시한다.
- 저장소 기준 커밋과 조사 환경을 기록한다.
- 로컬 근거는 `path:line`과 심볼을, 외부 근거는 원문 URL·발행/수정일·접근일을 기록한다.
- 사실, 관찰, 추론, 권고를 구분하고 각 핵심 Finding에 Source ID를 연결한다.
- 반대 근거, 불확실성, 재현 실패, 미해결 질문, 정보의 유효기한을 숨기지 않는다.
- 가능한 대안과 기각 이유, 제품·보안·성능·호환성·테스트·배포 영향을 다룬다.
- 계획 담당자가 바로 사용할 수 있도록 권고(`REC-NNN`)와 검증 가능한 인수 조건을 제공한다.
- 변동 가능성이 있는 사실은 계획 시점에 다시 확인한다. `review_by`가 지난 보고서는 갱신하거나 `needs-refresh`로 표시한다.

자세함은 글자 수가 아니라 의사결정에 필요한 근거와 반증 가능성의 완결성으로 판단한다. 출처나 실험 결과를 추측해서 채우지 않는다.

## 계획과 실행 기록

- 계획 파일: `docs/plans/P-NNN.md`
- 실행 파일: `docs/execution/E-NNN.md`
- 계획은 Research traceability 표에서 `R-ID/F-ID → 결정 → 변경 경로 → 검증`을 추적 가능하게 만들고, 검증·인수 조건마다 안정적인 `AC-NNN`을 부여한다.
- 실행 기록은 계획 대비 실제 결과를 남긴다. 완료 시 계획의 모든 AC-ID가 정확히 한 번 PASS여야 한다. 새 범위가 생기면 관련 연구와 계획을 먼저 개정한다.
- `main.js`는 커밋되는 배포 산출물이다. 소스 변경으로 다시 빌드했다면 실행 기록에 생성 여부와 diff 검토 결과를 남긴다.
- 테스트 산출물이 `build/`에만 남으면 저장소에서 사라지므로, 핵심 결과 요약은 실행 기록에도 보존한다.
- `AGENTS.md`, 거버넌스 검사기·workflow·템플릿 같은 governance-core 변경은 저장소 소유자가 작성하거나 현재 PR head에 승인해야 한다. 태그는 같은 커밋의 성공한 Governance check가 있어야 한다.

## 명령

```bash
npm run research:new -- --title "조사 제목" --topic "decision-topic-key"
npm run research:sync
npm run plan:new -- --title "계획 제목" --research R-001,R-002
npm run execution:new -- --title "실행 제목" --plan P-001
npm run governance:check -- --base HEAD
npm run governance:smoke
```

`REGISTER.md`의 자동 생성 구간은 직접 편집하지 않는다. 보고서 frontmatter를 고친 뒤 `npm run research:sync`로 재생성한다. 세부 사용법과 상태 전이는 각 디렉터리의 README를 따른다.
