## Dense Body

Semantic layout should split long prose by measured sentence height. This paragraph is intentionally verbose so that the engine has to observe wrapped lines instead of trusting character counts. The chosen layout should remain explainable in debug output. The source order should stay intact. The renderer should avoid forcing everything into one decorative surface.
