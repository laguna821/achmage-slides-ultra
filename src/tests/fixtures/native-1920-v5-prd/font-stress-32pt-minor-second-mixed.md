## Mixed Evidence At 32pt

Settings stay close to plain markdown so the user can move them between editors.

- Source remains markdown.
- Output is disposable.
- Layout reflects content.

```ts
const profile = {
  baseFontSize: 32,
  typographicScale: "minorSecond",
  surfaceMode: "cards",
};
```
