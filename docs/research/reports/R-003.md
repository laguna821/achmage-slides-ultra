---
id: R-003
title: "Community Plugin Scorecard Review 저위험 개선 전략 및 릴리스 게이트"
topic: "community-plugin-scorecard-review-hardening"
status: complete
created: 2026-08-02
updated: 2026-08-04
review_by: 2026-09-01
baseline_commit: "e49b5028ad68c55f39eef08405ae78d2971b15ea"
related:
  - R-002
supersedes:
---

# R-003 — Community Plugin Scorecard Review 저위험 개선 전략 및 릴리스 게이트

## Executive summary

- **2026-08-04 공개 `1.1.2` 후속 판정:** 개선은 실제로 반영됐다. 공개 ledger는 `1.0.3`의 `Warnings 222 + Other 3 = 225 issues`에서 `Warnings 50 + Other 2 = 52 issues`로 줄었고 Risks는 계속 0이다. issues는 173건(76.9%), warnings는 172건(77.5%) 감소했으며 dependency 검사는 disclosure에서 `No vulnerable dependencies found` pass로 이동했다. 그러나 Review는 계속 `Caution`이고 막대도 4칸 중 2칸이다. 공식 산식은 여전히 공개되지 않았지만, 공식 registry의 6,314개 중 결정론적으로 뽑은 512개 공개 page 표본(509개 파싱 성공, scan Risks 0인 457개)에서는 `Warnings 0 = Passed`, `1~30 = Satisfactory`, `31 이상 = Caution 또는 Risks`라는 경계에 반례가 없었다. 따라서 현재 50에서 막대를 한 칸 올리려면 새 경고 없이 최소 20건을 줄여 30 이하로 내려가야 한다. 비-CSS 8건을 전부 없애도 42라서 부족하고, 잔여의 84%인 `!important` 42건이 실질적인 병목이다. 이 막대는 warning 감소율을 연속적으로 보여주는 progress bar가 아니라 Review tier를 네 단계로 표시하는 categorical indicator로 보는 것이 현재 공개 관찰과 일치한다(SRC-025~SRC-030, F-021~F-025).
- **결론:** 공개 Scorecard의 `Review: Caution`은 현재 공개된 `Risk` 때문이 아니다. 최신 공개 릴리스 `1.0.3`의 Review 항목은 `Warnings 222 + Other 3 = 225 issues`, `Risks 0`이다. 225건 중 193건(85.8%)은 `unnecessary type assertion 151 + styles.css의 !important 42`라는 두 반복 패턴이다. 따라서 플러그인 아키텍처나 핵심 Pretext overflow 동작을 바꾸지 않고도 대부분을 줄일 수 있다.
- **권장 전략:** 한 번에 모든 라벨을 없애려 하지 말고, (A) 런타임 무동작 또는 극저위험 타입·콜백·dead-code 정리, (B) lockfile 보안 패치, (C) 시각 회귀 검증을 동반한 CSS specificity 정리, (D) 별도 build 실험으로 bundle size와 dependency 유래 `fs`를 처리하는 순서가 가장 안전하다. A만으로 이론상 171건, B까지 179건을 줄일 수 있다. C, browser build, 아직 찾지 못한 기능보존 size 해법까지 모두 성공하면 산술상 223건 제거가 가능하지만 이는 실증된 경로도 등급 보장도 아니다.
- **점수의 한계:** 2026-08-02에 검토한 공식 자료에서는 `Passed / Satisfactory / Caution / Risks`의 산식·가중치·임계값을 발견하지 못했다. 공식 ESLint도 Scorecard의 일부만 재현한다. 따라서 “N건을 없애면 Passed”를 인수 조건으로 쓰면 안 된다. **동일 candidate commit의 Developer Dashboard preview scan**은 필수 사전 게이트로 사용하되, 공개 릴리스 scan과 완전히 동등하다는 공식 보장은 없으므로 publish 후 public scan을 최종 판정으로 기록한다.
- **보존해야 할 기능:** clipboard write, export용 `requestUrl`, runtime base64는 사용자 요청에 의해 실행되는 실제 기능이다. 공개 detail의 network 28 flagged locations는 번들된 KaTeX parser의 비-HTTP `.fetch()` 명칭에 매핑됐다. 다만 명시적 first-party network API callsite가 export의 `requestUrl` 한 곳이라는 사실이 “제품의 네트워크 경로가 하나뿐”이라는 뜻은 아니다. Tier 3가 켜진 preview의 원격 R2/user override 배경과 Markdown 외부 이미지도 iframe/browser resource load를 일으킬 수 있다. 직접·간접 경로를 모두 정확히 disclosure한다.
- **동적 코드:** Dynamic Code Execution은 자체 overflow audit의 `iframe.contentWindow.eval(...)`과 번들된 `source-map-js`의 `new Function` 두 원인이 겹친다. 자체 `eval`은 deterministic overflow closed loop의 핵심 검증 경로이므로 이번 저위험 Scorecard 정리에서 제거하지 않는다. 한편 in-memory build 실험에서 esbuild를 browser platform으로 전환하면 dependency 유래 `fs`와 `new Function`이 사라지고 79개 render fixture가 동일했지만, 실제 Obsidian live smoke가 없으므로 아직 채택 결론이 아니다.
- **bundle size:** canonical Git blob·release asset의 `main.js`는 5,248,205 bytes다. 이는 5 MiB보다 5,325 bytes, 5,000,000 bytes보다 248,205 bytes 크다. 5,254,896 bytes는 Windows CRLF checkout 측정값이며 release artifact 크기로 사용하면 안 된다(F-016). Obsidian 공식 문서는 Sync Standard의 한계를 “5 MB”라고만 쓰며 scanner의 byte 임계값은 공개하지 않는다. 기능보존 해법을 찾는 size tranche에서는 `< 4,900,000 bytes`를 목표로 하되 현재 달성한 무손실 해법은 없어 무조건적 release AC로 삼지 않는다.
- **성능 조사와의 경계:** 작은 bundle은 설치·동기화와 일부 parse 비용에는 유리할 수 있지만, 이 보고서의 실험은 cold start, plugin load, first preview, first render, memory를 측정하지 않았다. **로딩 속도·경량화·eager/lazy import·메모리 최적화는 별도 R-ID(R-004 후보)**로 조사한다. R-003은 Scorecard를 줄이는 과정에서 얻는 저위험 size 이득까지만 다룬다.
- **릴리스 안전성:** 새 버전은 자동 검토되며 실패 시 검색 노출에 영향을 줄 수 있다. UX 연구 R-002의 변경과 본 보고서의 정리를 같은 후보 브랜치에서 통합하더라도 변경을 독립 commit/tranche로 유지한다. version과 committed build artifact를 candidate commit에 먼저 확정하고, 그 exact SHA를 preview scan한 뒤 tag가 같은 SHA를 가리키는지 확인하고 tag checkout에서 draft assets를 build/attest하여 publish한다. 공개 scan은 다시 비교한다.
- **남은 불확실성:** 공개 Scorecard 자체가 work in progress이며 공식 안내도 false positive/negative 가능성을 인정한다. Malware, vulnerable-dependency, obfuscation, network scans의 `not available` disclosure는 저장소에서 고칠 방법이 공개되어 있지 않다. preview scan으로도 근거가 맞지 않으면 commit SHA, lint 결과, dependency graph와 최소 재현을 보존해 `#plugin-dev`에 제기해야 한다.

## Research brief

### 배경과 의사결정

Achmage Slides Ultra는 2026-08-02 현재 Community Plugin 페이지에서 Health는 `Excellent`, Review는 `Caution`으로 표시된다. 기능상 설치가 차단된 상태는 아니지만, 공개 scorecard의 상태 라벨은 사용자가 설치 전에 보는 신뢰 휴리스틱이다. 제품 소유자는 R-002의 Open Slide Preview navigation discoverability 개선을 새 브랜치·새 릴리스로 배포할 때 Review 경고도 최대한 줄이고 싶어 한다. 단, 핵심 아키텍처, deterministic Pretext measurement, overflow-free packing 및 presentation 결과를 점수 때문에 훼손해서는 안 된다.

이 보고서는 다음 의사결정을 가능하게 한다.

1. 공개 Scorecard의 모든 Review 항목은 무엇이며 225라는 수가 어떻게 구성되는가?
2. 각 항목은 자체 코드, vendored Pretext, transitive dependency, 의도된 capability, scanner/platform 상태 중 어디에서 생기는가?
3. 플러그인 아키텍처와 핵심 렌더 의미를 보존하면서 제거 가능한 항목은 무엇인가?
4. 제거 비용과 회귀 위험을 고려한 구현 순서는 무엇인가?
5. 실제 등급을 추측하지 않고 새 branch와 release를 안전하게 검증하는 공식 경로는 무엇인가?
6. Scorecard 정리와 성능·경량화 연구를 어디까지 결합하고 어디서 분리해야 하는가?

### 핵심 질문

1. `Passed`, `Disclosures`, `Risks`, `Warnings`, `Other` 각각의 공개 항목과 개수는 정확히 무엇인가?
2. `225 issues`에 Disclosure가 포함되는가?
3. `Caution`의 공개 산식 또는 경고 수 임계값이 존재하는가?
4. 공식 `eslint-plugin-obsidianmd`로 어떤 항목을 로컬 재현할 수 있고 어떤 항목은 재현할 수 없는가?
5. 151개의 assertion과 42개의 `!important`는 193개의 독립 결함인가, 반복된 몇 개 패턴인가?
6. dependency advisory 8이라는 수는 8개의 고유 취약점인가, dependency path/occurrence인가?
7. `Direct Filesystem Access`, `Dynamic Code Execution`, `28 network calls`, base64, clipboard는 실제 제품 동작인가 dependency/lexical 탐지인가?
8. `main.js > 5 MB`를 기능 삭제나 패키징 아키텍처 변경 없이 해소할 수 있는가?
9. 새 릴리스를 공개하기 전에 동일 commit을 공식 scanner로 검증할 수 있는가?
10. “Scorecard 개선”과 “실제 로딩 성능 개선”을 하나의 연구 성공 기준으로 묶어도 되는가?

### 반증 질문

1. 경고를 많이 제거해도 비공개 가중치 때문에 `Caution`이 유지될 수 있는가?
2. `!important`를 제거해 외부 테마에서 slider가 다시 보이지 않게 되는가?
3. Type assertion을 단순 삭제할 때 타입 안전성이 오히려 약화되는 위치가 있는가?
4. dependency patch가 Marp/Markdown/PostCSS/MathJax 렌더 결과를 바꾸는가?
5. browser-platform build가 headless fixture에는 동일해도 실제 Electron/Obsidian runtime에서 깨지는가?
6. bundle 크기 감소가 실제 plugin load나 first preview를 개선하지 않을 수 있는가?
7. capability를 제거해 점수는 좋아지지만 export 또는 deterministic overflow audit 기능이 퇴행하는가?

### 성공 기준

- 공개 `1.0.3`의 225개 항목이 category 및 rule별 산술로 완전히 reconcile된다.
- 모든 자체 코드 경고에 `path:line` 또는 명확한 symbol이 연결된다.
- vendored, transitive, first-party, intentional capability, platform-unavailable을 구분한다.
- 사실, 관찰, 실험 결과, 추론, 권고를 분리하고 핵심 Finding마다 Source ID를 연결한다.
- 등급 산식, 5 MB threshold, advisory DB drift, false positive 가능성을 숨기지 않는다.
- “아키텍처 비변경” 범위에서 실행 가능한 단계와 보류할 단계를 구분한다.
- 다음 계획이 `F-ID → 결정 → 변경 경로 → 검증`으로 옮길 수 있는 권고와 acceptance 후보를 제공한다.
- 릴리스 전에 공식 preview scan을 요구하고 attestation pass를 보존한다.

## Scope

### 포함

- 2026-08-02 공개 Community Scorecard의 Achmage Slides Ultra `1.0.3` Review 전 항목
- Health의 Review 관련 맥락과 `CONTRIBUTING.md` 누락의 별도성
- TypeScript, CSS, dependency advisory, bundle size, filesystem, clipboard, dynamic code, network, base64 항목
- official ESLint, Developer Dashboard preview scan, GitHub release 및 artifact attestation 절차
- current `main.js`, source tree, package-lock, build/release configuration의 정적 분석
- product files를 쓰지 않는 in-memory bundle 실험과 기존 fixture render parity 비교
- R-002 UX 업데이트와 동일 릴리스에 묶을 때의 sequencing 및 regression boundary
- 실제 성능 연구를 별도 R-ID로 분리해야 하는 기준

### 제외

- 제품 코드, stylesheet, dependency lockfile, build config의 실제 수정
- branch 생성, version bump, tag, push, release publish
- 인증된 Developer Dashboard에서 candidate preview scan을 실제 실행하는 것
- public status 산식의 역공학을 사실로 단정하는 것
- Pretext overflow algorithm, slide parsing, navigation model의 재설계
- cold start, plugin load, first preview/first render, memory profile의 성능 계측
- math engine, rare syntax highlighting language, bundled font/background 기능의 삭제 결정
- scanner 회피를 위한 난독화, API 이름 바꾸기 또는 capability 은폐

## Baseline and method

- **저장소 기준:** `b061ce8c4aa528cb6b909f64a87a78a342b2f1d9`, branch `main`, manifest/package version `1.0.3`.
- **작업트리 상태:** R-001 거버넌스 시스템 구축에서 생긴 미커밋 문서·workflow·package script 변경이 이미 존재한다. R-003은 제품 source/build/release 파일을 수정하지 않았으며, README와 release workflow에 대한 관찰은 해당 선행 변경을 포함한 현재 작업트리를 기준으로 한다. 구현 계획에서는 pristine HEAD와 선행 governance diff를 구분해야 한다.
- **공개 기준:** Community detail page가 가리키는 latest release `1.0.3`; created 2026-06-26, last updated 2026-07-01, 조사 접근일 2026-08-02.
- **조사 환경:** Windows/PowerShell, Node `v24.12.0`, npm `11.6.2`. 조사 시작 당시 package-lock 및 설치 graph는 Marp Core `4.3.0`, Marpit `3.2.1`, js-yaml `4.1.1`, markdown-it `14.1.1`, linkify-it `5.0.0`, postcss `8.5.9`, xmldom `0.9.9`, esbuild `0.24.2`였다.
- **공개 페이지 수집:** Community Scorecard의 접힌 모든 Review details를 열어 제목, count, detail, file/line, package name을 전사했다.
- **로컬 매핑:** `rg`, source inspection, package-lock dependency path, production bundle token provenance, file-size 측정으로 각 항목을 매핑했다.
- **검증 명령:** `npm run typecheck` PASS; `npm audit --json`, `npm outdated --json`, `npm view`, `npm ls --all`; `Get-Item main.js`; esbuild `write:false` metafile 및 render-output hash 비교.
- **실험:** original lock 환경에서 node-platform과 browser-platform in-memory bundle을 비교했고, 79 fixture × MathJax/KaTeX render 결과 hash mismatch가 0인지 확인했다. 정확한 protocol은 본문에 기록했지만 inline script/output artifact를 보존하지 않았으므로 provisional이며 DOM live preview나 Electron smoke를 대체하지 않는다.
- **외부 근거:** Obsidian 공식 blog/help/developer docs, `obsidianmd` 공식 repositories, GitHub Advisory 원문만 규칙·릴리스 사실의 근거로 사용했다. 다른 플러그인 Scorecard는 issue 수만으로 상태가 정해지지 않는다는 관찰 표본으로만 사용했다.
- **재현성 주의:** 조사 중 `npm audit fix --dry-run --json`이 npm 11.6.2에서 예상과 다르게 ignored `node_modules`를 patch 버전으로 바꾸는 부작용이 발생했다. tracked `package.json`, `package-lock.json`, `main.js`는 변하지 않았다. 따라서 dependency/size 실험 수치는 “원래 lock 환경”과 “변경된 ambient node_modules”를 분리해 기록하고, 구현 단계에서는 반드시 `npm ci`로 lockfile 기준 clean install을 만든다.
- **방법의 한계:** Community scanner/backend source는 공개되어 있지 않고 검토한 공식 자료에서 rating formula를 발견하지 못했다. 인증된 owner dashboard preview scan을 이번 조사에서 실행하지 않았고, public Scorecard snapshot은 이후 scanner/advisory DB 변경을 반영하지 않을 수 있다.

## Current-state evidence

### 공개 프로젝트 상태

| 표면 | 공개 값 | 해석 |
|---|---:|---|
| Downloads | 182 | 조사 시점 누적치이며 변동 가능 |
| Current version | 1.0.3 | 이 보고서가 감사한 release |
| Created | 2026-06-26 | 공개 page metadata |
| Last updated | 2026-07-01 | 공개 page tooltip의 정확한 날짜 |
| Health | Excellent | Review와 별도 축 |
| Review | Caution | 검토한 공식 자료에서 산식을 발견하지 못함 |
| Installations | 59 | 조사 시점 adoption 값 |
| Review issues | 225 | `Warnings 222 + Other 3` |

Health의 Hygiene에는 readme, license, description이 존재하고 contributing guide만 없다. 이는 저비용 개선 후보지만 Review의 225 issues와 `Caution`을 직접 구성하는 항목으로 관찰되지 않았다.

### 전체 Review ledger

#### Passed — 5

| ID | 공개 항목 | 로컬/배포 의미 |
|---|---|---|
| PASS-001 | Build verified against source | source와 release build 일치 검증 통과 |
| PASS-002 | `main.js` artifact attestation verified | 현재 release provenance 통과 |
| PASS-003 | `styles.css` artifact attestation verified | 현재 release provenance 통과 |
| PASS-004 | Vault Read via API | Obsidian API 경유 read |
| PASS-005 | Vault Write via API | Obsidian API 경유 write |

#### Disclosures — 6

| ID | 공개 항목 | detail | 저자 조치 가능성 |
|---|---|---|---|
| DISC-001 | Number of network request calls | `28 network calls` | public flagged locations의 실제 provenance와 별도 browser load를 설명; count 자체는 source/dependency에 좌우 |
| DISC-002 | Runtime base64 encode/decode | `atob`/`btoa` 계열 | export self-containment 기능이므로 정직하게 disclosure |
| DISC-003 | Malware scan not available | scanner result unavailable | 공개된 저장소 조치 없음 |
| DISC-004 | Vulnerable dependencies scan not available | scanner result unavailable | warning의 advisory cleanup과 별개 |
| DISC-005 | Obfuscation scan not available | scanner result unavailable | 공개된 저장소 조치 없음 |
| DISC-006 | Network requests scan not available | scanner result unavailable | 공개된 저장소 조치 없음 |

`Disclosures`는 `225 issues` 산술에 포함되지 않는다. 공개 scorecard는 “Work in progress; may contain errors”라고 경고하며, 공식 블로그도 false positive/negative 가능성을 명시한다.

#### Warnings — 222

| ID | 공개 rule/항목 | 개수 | 누적 | 주요 소유권 |
|---|---|---:|---:|---|
| W-001 | Unnecessary assertion: does not change expression type | 151 | 151 | vendored Pretext 147, first-party 4 |
| W-002 | Avoid `!important` | 42 | 193 | `styles.css` 한 UI cluster |
| W-003 | Dependency has a potential vulnerability advisory | 8 | 201 | transitive Marp graph/lockfile |
| W-004 | Promise returned where void expected | 5 | 206 | first-party DOM event callbacks |
| W-005 | Use `activeDocument` instead of `document` | 5 | 211 | vendored Pretext measurement |
| W-006 | Unsafe argument | 3 | 214 | first-party 3 |
| W-007 | Unsafe assignment | 3 | 217 | first-party 2, vendored 1 |
| W-008 | Unsafe member access | 1 | 218 | first-party dev-only branch |
| W-009 | Unnecessary assertion on receiver | 1 | 219 | vendored Pretext |
| W-010 | Use of `globalThis` | 1 | 220 | vendored Pretext headless compatibility |
| W-011 | `main.js` from 1.0.3 is greater than 5 MB | 1 | 221 | release artifact/build graph |
| W-012 | Direct Filesystem Access | 1 | 222 | bundled PostCSS `require("fs")` |

#### Other — 3

| ID | 공개 항목 | 로컬 provenance | 판단 |
|---|---|---|---|
| O-001 | Clipboard Access | `src/settings.ts:450,468`, explicit button-triggered write | intentional capability, 유지 |
| O-002 | Dynamic Code Execution | first-party `win.eval` + dependency `new Function` | 핵심 audit와 dependency 양쪽; 이번 범위 보류 |
| O-003 | Unused `transformTables` | `src/preprocessor/shapeTemplates.ts:1306-1331` | dead code, 제거 후보 |

`222 + 3 = 225`이며 Passed 5와 Disclosures 6은 이 합계에서 제외된다. 공개 Risks는 0이다.

### W-001: assertion 151건의 실제 분포

| 경로 | 개수 | 분류 | 비고 |
|---|---:|---|---|
| `src/engine/slideRenderer.ts:1012` | 1 | first-party | heading template-literal type |
| `src/measurement/PretextMeasurementEngine.ts:238` | 1 | first-party | iterator result; `done` 검사 형태가 더 안전 |
| `src/preprocessor/shapeTemplates.ts:370,379` | 2 | first-party | 앞선 narrowing/구조 타입으로 충분 |
| `src/vendor/pretext/analysis.ts` | 80 | vendored | 78 unique lines; public list에 884, 1061 중복 occurrence |
| `src/vendor/pretext/bidi.ts` | 14 | vendored | type-only assertions |
| `src/vendor/pretext/layout.ts` | 16 | vendored | type-only assertions |
| `src/vendor/pretext/line-break.ts` | 29 | vendored | type-only assertions |
| `src/vendor/pretext/line-text.ts` | 3 | vendored | type-only assertions |
| `src/vendor/pretext/measurement.ts:117` | 1 | vendored | regex capture non-null assertion |
| `src/vendor/pretext/rich-inline.ts` | 4 | vendored | type-only assertions |
| **합계** | **151** | **vendor 147 / own 4** | vendor 비율 **97.35%** |

이 중 대부분은 TypeScript compile-time 표현이며 production JS에는 assertion 자체가 남지 않는다. 다만 `PretextMeasurementEngine.ts:238`은 assertion만 삭제하면 iterator `.value`가 `any`로 약화될 수 있으므로 `IteratorResult.done` 검사 또는 first-key helper로 바꿔야 한다. 정규식으로 `!`를 일괄 삭제하면 logical negation을 훼손할 수 있으므로 공식 type-aware lint/AST fix 또는 개별 검토가 필요하다.

별도 W-009는 `src/vendor/pretext/layout.ts:560`의 `prepareInternal(...) as PreparedText` 한 건이다. 같은 파일의 다른 cast까지 근거 없이 제거해서는 안 된다.

### W-002: `!important` 42건의 한정된 UI 표면

42개는 전부 release `styles.css`의 settings/quick typography slider 표면에 집중된다. TS에서 생성하는 slide theme CSS의 `!important`는 이 공개 warning count에 포함되지 않았다.

| line | 개수 | UI 상태 |
|---|---:|---|
| `styles.css:155-156` | 2 | reset button |
| `styles.css:205-210` | 6 | range input base |
| `styles.css:216-219` | 4 | WebKit track |
| `styles.css:228-238` | 11 | WebKit thumb |
| `styles.css:243-244` | 2 | WebKit active thumb |
| `styles.css:250-253` | 4 | Firefox track |
| `styles.css:260-267` | 8 | Firefox thumb |
| `styles.css:272-273` | 2 | Firefox active thumb |
| `styles.css:279` | 1 | focus outline reset |
| `styles.css:283,287` | 2 | focus ring |
| **합계** | **42** | 하나의 typography control cluster |

`styles.css:196-201`의 주석은 community theme가 range control을 덮어 white-on-white가 되는 것을 막기 위해 `!important`를 의도적으로 사용했다고 설명한다. 단순 삭제는 위험하다. owner container + dedicated class + pseudo-element로 specificity를 올리는 방식이 아키텍처에는 영향을 주지 않지만, 상대 theme도 `!important`를 사용하면 이길 수 없으므로 실제 theme matrix 검증이 필수다.

### W-003: release snapshot과 현재 advisory DB의 차이

공개 Scorecard는 8 occurrence와 다음 5개 package name을 표시한다.

| package-lock 위치 | release lock version | 유입 경로 |
|---|---:|---|
| `package-lock.json:648` | `@xmldom/xmldom@0.9.9` | Marp → MathJax → speech-rule-engine |
| `package-lock.json:808` | `js-yaml@4.1.1` | Marp → Marpit |
| `package-lock.json:836` | `linkify-it@5.0.0` | Marp → Marpit → markdown-it |
| `package-lock.json:851` | `markdown-it@14.1.1` | Marp → Marpit |
| `package-lock.json:964` | `postcss@8.5.9` | Marp → Marpit/PostCSS plugins |

공개 detail에서 확인 가능한 고유 advisory link는 PostCSS의 `GHSA-qx2v-qp2m-jg93` 하나다. 해당 advisory는 `postcss < 8.5.10`에 영향을 주고 8.5.10에서 수정됐다. 그러므로 badge의 8을 “서로 다른 고유 CVE 8개”로 단정할 수 없다. occurrence, dependency path 또는 당시 DB 결과일 수 있으나 공개 정의가 없다.

2026-08-02 원래 lock 환경의 `npm audit --json`은 7개 affected package metadata(Moderate 2, High 5)를 보고했다. 여기에는 public release Scorecard가 열거하지 않은 dev-only `esbuild@0.24.2` advisory도 포함되고, xmldom에는 공개 release 이후 갱신된 advisory가 포함된다. 즉 두 결과는 서로 다른 시점·scope의 scan이며 모순이 아니다.

비강제 `npm audit fix` preview는 declared Marp range를 유지한 채 SRE `4.1.4`, xmldom `0.9.10`, postcss `8.5.25`, markdown-it `14.3.0`, linkify-it `5.0.2`, js-yaml `4.3.1`로 올리는 해법을 제시했다. esbuild는 `0.28.1` major update가 별도 필요하다. 이 결과는 아직 lockfile에 반영된 수정이 아니며, 구현 시 clean `npm ci` → 의도적 lock update → clean `npm ci` → audit/acceptance 순서로 재현해야 한다.

### W-004~W-010 및 O-003: 저위험 source hygiene 위치

| rule | 정확한 위치 | 최소 변경 방향 | 회귀 위험 |
|---|---|---|---|
| Promise→void 5 | `src/settings.ts:192`; `src/view/slidePreviewView.ts:498-504,522,530-535,537-544` | sync event wrapper 안에서 async 작업을 명시적으로 `void`; await 순서는 유지하고 rejection 정책은 별도 결정 | 매우 낮음 |
| activeDocument 5 | `src/vendor/pretext/measurement.ts:43,146,148,156,158`; guard `:42,145`도 함께 검토 | 한 번 해석한 `doc`를 guard/create/body/append/remove에 일관 사용 | 낮음~중간 |
| unsafe argument 3 | `src/preprocessor/obsidianCompat.ts:55`; `shapeTemplates.ts:1018,1024` | callback boundary narrowing, `Array<number>` | 매우 낮음 |
| unsafe assignment 3 | `src/preprocessor/overflowSplitter.ts:406,452`; `vendor/pretext/bidi.ts:40` | `Array<string>`, `Array<BidiType>` | 매우 낮음 |
| unsafe member 1 | `src/engine/slideRenderer.ts:1033` | typed build constant 또는 process shape; prod branch tree-shake 확인 | 낮음 |
| receiver assertion 1 | `src/vendor/pretext/layout.ts:560` | 해당 cast만 제거 | 매우 낮음 |
| globalThis 1 | `src/vendor/pretext/measurement.ts:89` | 앞선 guard를 유지하고 lexical `navigator` 사용 | 낮음, headless 검증 |
| unused 1 | `src/preprocessor/shapeTemplates.ts:1306-1331`; 전용 helper `:1290-1296`; stale comment `:1462` | `transformTables`, 전용 `wrapTableCard`, 낡은 disabled-comment 함께 제거 | 매우 낮음 |

Promise 경고는 native DOM listener만 잡힌 것이다. 예시처럼 wrapper 자체는 `void`를 반환해야 한다.

```ts
element.addEventListener("change", () => {
  void commitInput();
});
```

`void`는 promise rejection을 자동 처리하지 않는다. 계획은 `.catch(...)`/공용 reporter로 명시적 처리할지, 기존 unhandled 동작을 그대로 보존할지를 결정해야 하며, catch를 추가한다면 그것도 관찰 가능한 동작 변경으로 기록한다.

### W-011: bundle size와 구성

현재 canonical committed `main.js` Git blob은 5,248,205 bytes이고 Windows CRLF checkout은 5,254,896 bytes다. `styles.css`는 8,572 bytes다.

| 기준 | 한계 | 초과량 |
|---|---:|---:|
| decimal MB 가정 | 5,000,000 | 254,896 bytes |
| binary MiB 가정 | 5,242,880 | 12,016 bytes |

공식 Sync 문서는 Standard의 maximum file size를 `5 MB`, Plus를 `200 MB`로 표기하지만 scanner의 단위 구현은 공개하지 않는다. 따라서 `5,242,879`처럼 경계만 통과하는 목표는 다음 UX 코드 추가나 minifier 변경으로 쉽게 재발한다.

원래 lock 환경의 esbuild metafile에서 대형 기여도는 다음과 같다.

| 묶음 | bytesInOutput | 현재 bundle 대비 대략적 비중 |
|---|---:|---:|
| MathJax | 1,785,773 | 34.0% |
| project `src` | 1,426,494 | 27.1% |
| Highlight.js | 1,074,744 | 20.5% |
| KaTeX | 272,009 | 5.2% |
| Marp core | 212,427 | 4.0% |
| PostCSS | 52,148 | 1.0% |
| source-map-js | 28,811 | 0.5% |

project `src`의 큰 정적 payload는 `src/assets/fonts.generated.ts` 666,921 bytes, `src/assets/tier3Backgrounds.generated.ts` 511,766 bytes, `src/themes/themeRegistry.ts` 100,667 bytes다. 원본 Pretendard WOFF2는 462,820 bytes, JetBrains Mono WOFF2는 36,980 bytes이며 background 7개는 WebP다. 이미 압축 형식이므로 재압축 이득은 측정 전 단정할 수 없다.

현재 release는 `main.js`, `manifest.json`, `styles.css` 세 자산만 배포한다. font/background를 별도 파일로 빼는 것은 단순 size tweak가 아니라 packaging/release contract 변경이므로 R-003의 권장 저위험 경로가 아니다.

#### build 후보 실험

| 후보 | in-memory 결과 | Scorecard 영향 추정 | 검증 결과 | 판정 |
|---|---:|---|---|---|
| current node platform | 5,248,205 bytes canonical Git blob; 5,254,896 bytes Windows CRLF checkout | size/fs 유지 | baseline | 현 상태 |
| browser platform + builtin external 정리 | raw 5,217,550 bytes; production rewrite 등가 5,224,251 bytes, committed 대비 30,645 감소 | binary 5 MiB 아래; dependency `fs`, `new Function` 제거 가능 | 79 fixtures × 2 math engines, 158 pair hash mismatch 0 | 탐색적·유망; live smoke 전 미채택 |
| 26개 공용 highlight language + 나머지 plaintext fallback | browser raw 4,285,064 bytes; rewrite 등가 추정 4,291,765 bytes | robust size margin | bundle-only 실험; rare grammar는 escaped plaintext 보존 | 탐색적; 희귀 highlighting 퇴행, 제품 정책 필요 |
| KaTeX 또는 MathJax 제거 | 수백 KB 이상 가능 | robust size margin | 미수행 | 기능 삭제, 기각 |
| asset 외부화 | `main.js` 감소 | packaging 변경 | 미수행 | 현재 아키텍처 범위 밖 |
| Marp fork/custom build | 큰 감소 가능 | dependency warnings도 제어 가능 | 미수행 | 유지비·아키텍처 영향 큼, 기각 |

탐색적 browser-platform bundle은 baseline과 같은 `entryPoints`, CJS, ES2022, production define, minify, tree-shaking, no sourcemap, `write:false`, metafile을 사용했다. 정확한 delta는 `platform: node → browser`와 external 목록에서 `builtinModules`를 제거한 두 가지이며 Obsidian/Electron/CodeMirror/Lezer externals는 유지했다. raw output 5,217,550 bytes에 `esbuild.config.mjs:10-14`의 stage-default rewrite를 메모리 적용하면 6,701 bytes 늘어 5,224,251 bytes였다.

Fixture protocol은 `src/engine/marpEngine.ts`를 node/browser 각각 in-memory CJS로 bundle해 test process에서만 `new Function` harness로 load한 뒤, `src/tests/fixtures/native-1920-v5-prd`의 정렬된 Markdown 79개를 MathJax와 KaTeX 각각 render하고 전체 result의 `SHA256(JSON.stringify(result))`를 비교했다. 158 pair 모두 mismatch 0이었다. 이 harness의 `new Function`은 release artifact에 들어가지 않는다. Inline experiment script와 output artifact는 저장소에 보존하지 않았으므로 이 결과는 **provisional**이며, 계획 실행 시 versioned script/JSON으로 다시 남겨야 한다. UI/iframe/stage rewrite/live Electron 검증도 아니다.

26-language experiment의 keep 목록은 `plaintext, javascript, typescript, python, java, c, cpp, csharp, go, rust, kotlin, swift, ruby, php, bash, shell, powershell, json, yaml, xml, css, scss, sql, markdown, dockerfile, diff`다. 그 외 `highlight.js/lib/languages/*` module을 빈 grammar로 resolve하여 code text/escaping은 보존하되 colored span은 없앴다. raw 4,285,064 bytes만 직접 계측했고 4,291,765 bytes는 같은 +6,701 rewrite를 가정한 등가치다.

browser-platform 실험은 module resolution/build semantics를 바꾸지만 제품 feature architecture를 바꾸지는 않는다. 그러나 Electron에서 Obsidian external, Node builtin 사용, source-map comment, custom theme CSS, HTML export, popout, cold start를 실제로 확인하기 전에는 적용하면 안 된다. 더구나 5,224,251 bytes는 5 MiB에는 들어가도 decimal 5 MB와 권고 budget에는 실패하므로 단독 해법이 아니다.

Highlight.js 26-language 후보는 크기 면에서는 가장 효과적이지만, 지원하던 code fence language를 plaintext로 퇴행시킨다. “사용하지 않는 코드 제거”로 가장하면 안 되며 supported-language 정책과 telemetry 없는 사용자 호환성 가정을 별도 결정해야 한다.

### W-012, O-002: filesystem과 dynamic code provenance

production entry dependency graph의 first-party non-test runtime source에는 Node `fs` import가 없다. `src/tests/overflowMatrix.ts`, `src/tests/realnoteCheck.ts`, `src/tests/tier2Dump.ts`와 build scripts에는 `node:fs`가 있지만 release bundle 밖이다. `main.js`의 유일한 `require("fs")`는 번들된 `postcss/lib/previous-map.js`의 source-map file lookup에서 온다. 현재 제품은 문자열 CSS를 처리하므로 그 경로가 실제 사용될 가능성은 낮지만 capability가 번들에 존재하는 것은 사실이다.

Dynamic Code Execution은 두 원인이다.

1. `src/audit/auditLoop.ts:135`의 `win.eval(buildProbeExpression(tolerancePx))`; same-origin iframe 안에서 실제 rendered DOM의 overflow를 닫힌 루프로 검사하는 first-party 핵심 경로.
2. `source-map-js/lib/quick-sort.js`의 comparator-specialized `new Function`; PostCSS source-map 지원에서 유입된 transitive 코드.

따라서 own eval만 제거해도 dependency `new Function`이 남고, dependency만 제거해도 own eval이 남는다. browser-platform 실험은 PostCSS/source-map-js의 Node path를 tree-shake해 `fs`와 `new Function`을 제거했지만 own `win.eval`은 남겼다. own eval을 없애려면 same-origin iframe `contentDocument`를 typed probe 함수로 직접 검사하거나 안전한 message/property bridge를 설계하고, 기존 overflow matrix 외에도 timing, font reflow, iframe teardown, stale-generation cancellation parity를 검증해야 한다. 이는 “Other 1개 제거”에 비해 핵심 동작 위험이 크므로 별도 연구/계획 없이는 수정하지 않는다.

### Capability disclosures의 실제 의미

| disclosure/other | 실제 provenance | 사용자 trigger | 권고 |
|---|---|---|---|
| Network 28 | public detail의 28 flagged locations가 KaTeX parser의 비-HTTP `.fetch()` call 27 + method definition 1에 매핑됨 | 해당 없음 | 로컬 매핑 근거 보존, dependency patch로 숨기지 않음 |
| 명시적 first-party network API | `src/main.ts:349` `requestUrl({url})` | remote background/image가 있는 HTML export | README의 기존 trigger, destination, data flow를 검증·보강 |
| browser resource load | `src/settingsDefaults.ts:9-24`의 R2 URL 7개(default Tier 3 off), `src/engine/slideRenderer.ts:1162-1174`의 remote override, Markdown 외부 `<img>` | Tier 3를 켜거나 remote resource가 든 slide를 preview | iframe/browser가 직접 load할 수 있음을 disclosure하고 online/offline test |
| Base64 | `src/main.ts:275` `btoa`; PostCSS source-map base64 helpers | local/remote image를 self-contained export에 inline | 기능 유지 및 disclosure |
| Clipboard | `src/settings.ts:450,468` write-only | plugin path/prompt copy 버튼 클릭 | 기능 유지 및 disclosure |
| Filesystem | PostCSS `require("fs")`; first-party는 Obsidian `FileSystemAdapter` | dependency path는 통상 미사용; local override는 설정 기능 | dependency path 제거 실험, 사용자 기능 disclosure |

README `:41-47`은 Tier 3 R2 live load와 export inline을, `:49-56`은 desktop-only 이유, `requestUrl`, `FileSystemAdapter`, OS file manager를 이미 공개한다. public “28 network calls”의 flagged-location mapping과 실제 direct API/iframe browser load가 서로 다른 측정임을 오해 없이 정밀화하고, clipboard write와 base64의 목적도 capability disclosure section에 명시하면 신뢰에 유리하다. 문서화가 등급을 자동 변경한다고 보장할 수는 없다.

### 공식 local lint와 Scorecard의 차이

2026-08-02 공식 `eslint-plugin-obsidianmd` 최신 package는 0.4.1이다. recommended config는 type-aware `typescript-eslint` 설정을 상속하므로 assertion, misused promises, unsafe argument/assignment/member access, unused vars 다수를 로컬 재현할 수 있다. TypeScript parser에 project를 연결하지 않으면 type-aware 결과가 달라질 수 있다.

그러나 공식 plugin의 `obsidianmd/prefer-active-doc`은 recommended에서 `off`인데 공개 Scorecard는 이를 warning으로 표시한다. official lint에는 `no-eval`, `no-new-func`, 조건부 `no-nodejs-modules` 등 일부 source-level capability/security 규칙이 있지만 Scorecard의 종합 capability/disclosure inventory를 재현하지 못한다. CSS, advisory, bundle size, artifact attestation 및 scan availability도 lint 범위 밖이다. 따라서 local gate는 최소 다음 두 층이어야 한다.

1. official recommended + project type information + `prefer-active-doc` overlay + 필요한 CSS/size/audit scripts.
2. candidate branch/tag/commit에 대한 Developer Dashboard preview scan.

2026-08-02에 검토한 공식 blog/docs와 공개 `obsidianmd` source에서는 Scorecard 전용 waiver file, suppress annotation, 공개 appeal form, repository-side scanner 설정을 발견하지 못했다. 공식 lint는 무제한 disable도 제한한다. false positive는 숨기지 말고 공식 `#plugin-dev`에 증거로 제기한다.

### rating formula에 대한 관찰과 한계

2026-08-02에 검토한 공식 문서와 공개 저장소에서는 status formula를 발견하지 못했다. 공개 플러그인 표본은 issue count 하나만으로 status가 결정되지 않음을 보여준다.

| plugin | Review | Risks | Warnings | Other | issues |
|---|---|---:|---:|---:|---:|
| Frontmatter Date Manager | Passed | 0 | 0 | 1 | 1 |
| Loft | Passed | 0 | 0 | 7 | 7 |
| OpenLoops Hidden Files | Satisfactory | 0 | 1 | 1 | 2 |
| Recent Files | Satisfactory | 0 | 7 | 1 | 8 |
| Markdown Table Editor | Caution | 1 | 2 | 0 | 3 |
| Achmage Slides Ultra | Caution | 0 | 222 | 3 | 225 |
| Personal OS | Risks | 21 | 1114 | 3 | 1138 |

이 표에서만 말할 수 있는 것은 Other가 있어도 Passed일 수 있고, Warning 1건부터 Satisfactory가 관찰되며, Risk 또는 매우 많은 Warning이 Caution과 연관된다는 점이다. 종류·심각도·수량 외 다른 factor가 집계될 수 있다는 **추론**은 가능하지만 정확한 경계는 알 수 없다. 경험적으로 Passed를 가장 안전하게 노리려면 Risks 0을 유지하고 Warnings 0에 접근해야 한다. Preview는 사전 근거로, publish 후 public scan은 최종 관찰로 기록한다.

현재 Achmage의 `Caution` page에도 `Add to Obsidian`이 노출된다. 공식 문서는 errors/fails review가 검색·다운로드에 미치는 영향은 설명하지만 `Caution`이라는 public summary label을 review failure와 동일하다고 정의하지 않는다. 따라서 **현재 Caution은 설치 차단과 동의어가 아니며**, 새 후보의 preview scan에서는 summary label과 별도로 errors/fail flags가 없는지를 확인해야 한다.

### 2026-08-03 릴리스 준비 재현

#### 격리된 1.0.3 dependency graph

tag `1.0.3`의 `package.json`과 `package-lock.json`만 임시 디렉터리에 추출해 Node `24.12.0`, npm `11.6.2`에서 다시 검사했다(SRC-016). 원본 lock은 package version과 달리 top-level `version`과 `packages[""].version`이 `1.0.1`이었고, production audit는 6건(Moderate 1, High 5), full audit는 7건(Moderate 2, High 5)이었다.

다음 비강제 lock-only 명령은 `package.json` byte를 바꾸지 않고 Marp/Marpit 버전을 유지하면서 production advisory를 0으로 만들었다.

```powershell
npm.cmd audit fix --package-lock-only --ignore-scripts --no-fund --json
```

명령 exit는 dev-only `esbuild@0.24.2` Moderate 1건이 남아 `1`이었다. 따라서 exit code만 보고 production patch가 실패했다고 해석하면 안 된다. 갱신 후 정확한 lock graph는 다음과 같았다.

```text
achmage-slides-ultra@1.0.3
├─ @marp-team/marp-core@4.3.0
│  ├─ @marp-team/marpit@3.2.1
│  │  ├─ @csstools/postcss-is-pseudo-class@5.0.3 ─┐
│  │  ├─ postcss-nesting@13.0.2 ─────────────────┼─ postcss@8.5.25 ─ nanoid@3.3.16
│  │  ├─ js-yaml@4.3.1                           │
│  │  └─ markdown-it@14.3.0 ─ linkify-it@5.0.2   │
│  └─ mathjax-full@3.2.2 ─ speech-rule-engine@4.1.4 ─ @xmldom/xmldom@0.9.10
└─ esbuild@0.24.2 (dev)
```

lock의 top-level/root version은 npm이 package version과 같은 `1.0.3`으로 정렬했다. 별도 dev-tool 실험에서 esbuild를 `0.28.1`로 올리자 full audit도 0이 됐다. 정확한 변화는 `speech-rule-engine 4.1.3→4.1.4`, `@xmldom/xmldom 0.9.9→0.9.10`, `js-yaml 4.1.1→4.3.1`, `linkify-it 5.0.0→5.0.2`, `markdown-it 14.1.1→14.3.0`, `postcss 8.5.9→8.5.25`, `nanoid 3.3.11→3.3.16`, 별도 `esbuild 0.24.2→0.28.1`이다. 이 snapshot은 registry/advisory DB에 따라 변하므로 실행 시 같은 명령과 graph를 재확인하고 달라지면 R-003과 계획을 먼저 갱신한다.

#### canonical `main.js`와 Windows checkout bytes

기존 본문의 `5,254,896 bytes`는 `core.autocrlf=true`인 Windows working tree의 CRLF-expanded 파일을 잰 값이었다. 이를 “committed artifact가 stale하다”로 해석하는 것은 틀리다(SRC-017).

| 표면 | bytes | SHA-256 | 판정 |
|---|---:|---|---|
| tag `1.0.3` Git blob | 5,248,205 | `a283d7bb3ad4dd8f05be3b678200b821405139a6b5140de010001881159612bd` | canonical LF artifact |
| GitHub release `1.0.3/main.js` | 5,248,205 | `a283d7bb3ad4dd8f05be3b678200b821405139a6b5140de010001881159612bd` | tag blob과 동일 |
| tag `1.0.3` 격리 clean build | 5,248,205 | `a283d7bb3ad4dd8f05be3b678200b821405139a6b5140de010001881159612bd` | 공개 asset과 동일 |
| Windows checkout raw bytes | 5,254,896 | `687b81801dbc4a6f96c35dd1d7ce55c0d46bf53a24f3b78c2b4e96502a7fd735` | CRLF-expanded working copy |
| Windows checkout LF-normalized | 5,248,205 | `a283d7bb3ad4dd8f05be3b678200b821405139a6b5140de010001881159612bd` | canonical과 동일 |

`git ls-files --eol main.js`는 `i/lf w/crlf`를 보고했고 `git diff --exit-code -- main.js`는 0이었다. 따라서 릴리스 검증은 checkout raw hash를 곧바로 공개 asset과 비교하지 말고 tag Git blob 또는 clean Linux/tag build의 canonical bytes를 사용한다. build 뒤에는 normalized Git diff와 release asset digest를 함께 확인한다.

#### 현재 지원 toolchain과 draft release

현재 release workflow 계열은 Node `20.x`를 사용하지만 Node.js 공식 표에서 v20은 2026-03-24 EOL이고 v24는 LTS다(SRC-018). 1.1.0 workflow는 `24.x`로 올려 EOL runtime을 release gate에서 제거한다.

Obsidian 공식 GitHub Actions 가이드는 manifest와 같은 no-`v` annotated tag를 만든 뒤 `gh release create ... --draft`로 `main.js`, `manifest.json`, `styles.css`를 첨부하고, workflow 성공 후 사람이 release notes를 작성해 Publish하도록 안내한다(SRC-019). 현행 1.0.3 workflow는 `--draft` 없이 바로 공개하므로 1.1.0은 attestation을 유지한 채 draft-only 생성으로 바꾼다.

제품 소유자가 승인한 release blocker는 exact candidate preview의 `Risks 0`, errors/fails 0, 그리고 공개 1.0.3 대비 전체 및 개별 Review category 비악화다. `Caution` summary label이 남는 것만으로 release를 막지 않는다. publish 뒤 public scan은 별도 최종 evidence로 기록한다(SRC-020).

### 2026-08-04 공개 `1.1.2` post-release 재검사

공개 Community page가 `Current version 1.1.2`를 가리키는 상태에서 Scorecard tab의 server-rendered 내용과 source link를 다시 수집했다(SRC-025). exact release는 annotated tag `1.1.2`가 dereference하는 merge commit `e49b5028ad68c55f39eef08405ae78d2971b15ea`이며, 공개 `main.js` asset과 tag blob은 모두 5,268,064 bytes다(SRC-028). 네 공개 tier의 server HTML을 대조하면 Passed는 4/4, Satisfactory는 3/4, Caution은 2/4, Risks는 1/4 segment를 활성화한다. 공개 client는 이미 계산된 label·색·icon·segment만 렌더하며 finding을 tier로 환산하는 산식은 포함하지 않았다(SRC-029).

| 지표 | 공개 `1.0.3` | 공개 `1.1.2` | 변화 |
|---|---:|---:|---:|
| Review | Caution | Caution | 같은 tier |
| Review bar | 2/4 | 2/4 | 같은 categorical 표시 |
| Review issues | 225 | 52 | -173 (-76.9%) |
| scan Risks | 0 | 0 | 유지 |
| Warnings | 222 | 50 | -172 (-77.5%) |
| warning category | 12 | 7 | -5 (-41.7%) |
| Other | 3 | 2 | -1 (-33.3%) |
| Passed | 5 | 6 | +1 |
| Disclosures | 6 | 5 | -1; vulnerable dependency가 pass로 이동 |

현재 공개 warning 50건은 다음 일곱 category로 정확히 합산된다.

| 공개 warning | 수 | exact release provenance | 제거 성격 |
|---|---:|---|---|
| Avoid `!important` | 42 | `styles.css:155-156,205-287`; reset 2 + range slider 40 | 실제 theme specificity/contrast/focus 호환성, 고위험 |
| Avoid unnecessary logging to console | 2 | `src/engine/slideRenderer.ts:1270,1275` | production build에서 tree-shake되는 dev assertion |
| Uses `document.createElement` instead of Obsidian `createEl` helpers | 2 | `src/vendor/pretext/measurement.ts:44,146` | captured document의 canvas/emoji 측정 경로 |
| `main.js` larger than 5 MB | 1 | release blob 5,268,064 bytes; 5 MiB보다 25,184 bytes 큼 | 기능보존 size tranche 필요 |
| Direct Filesystem Access | 1 | bundled `postcss/lib/previous-map.js`의 `require("fs")`; first-party FileSystemAdapter capability도 별도 존재 | build/runtime compatibility 검증 필요 |
| Unsafe member access on `error`/`any` | 1 | `src/engine/slideRenderer.ts:1268`의 dev-only `process.env` guard | 위 console 2건과 같은 tree-shaken block |
| `PluginSettingTab` lacks `getSettingDefinitions()` | 1 | `src/settings.ts:104`와 imperative `display()` | Obsidian 1.13+ settings search; dual-support 설계 필요 |

`42 + 2 + 2 + 1 + 1 + 1 + 1 = 50`이다. Other는 Clipboard Access와 Dynamic Code Execution 두 건이며, 둘 다 기존 보고서에서 기능 또는 핵심 overflow audit 경로로 분류한 항목이다. Candidate preview의 48개보다 public warning이 2개 많은 것은 release asset이 있어야 판정 가능한 size와 bundled Direct FS가 public scan에 추가된 결과와 일치한다.

#### Review tier 경계 표본

공식 `obsidianmd/obsidian-releases`의 `community-plugins.json` commit `3ffa42609e951897258669e13847271a015fada4`에는 6,314개 plugin이 있었다. registry 순서를 유지한 채 `floor(i × (N-1)/(K-1))`, `i=0..511`, `K=512`로 동일 간격 표본을 고르고 각 공개 page의 Next.js server payload에서 Review label, total issues, Risks/Warnings/Other section count를 읽었다(SRC-026). 512개 중 509개가 파싱됐고 3개는 HTTP 200이지만 Review block을 찾지 못해 제외했다. 성공 표본의 분포는 다음과 같다.

| Review label | 전체 509 | scan Risks = 0 | scan Risks > 0 |
|---|---:|---:|---:|
| Passed | 124 | 124 | 0 |
| Satisfactory | 229 | 229 | 0 |
| Caution | 137 | 99 | 38 |
| Risks | 19 | 5 | 14 |
| 합계 | 509 | 457 | 52 |

여기서 Review label `Risks`와 scan의 `Risks` section count는 같은 변수가 아니다. scan Risks가 0이어도 warnings가 매우 많은 표본 5개는 Review label `Risks`였다. Achmage와 직접 비교 가능한 scan Risks 0 표본에서는 다음 경계가 관찰됐다.

| 관찰 | 값 | 경계 예시 |
|---|---:|---|
| Passed의 최대 Warnings | 0 | Full Calendar Remastered는 Warnings 0, Other 9, Passed |
| Satisfactory의 최대 Warnings | 30 | Vim IM Select·Plotly·Finnish Spellcheck |
| Caution의 최소 Warnings | 31 | Evernote Decryptor·Memoria |
| Satisfactory이면서 Warnings > 30 | 0건 | 표본 내 반례 없음 |
| Caution이면서 Warnings ≤ 30 | 0건 | scan Risks 0 subset에서 반례 없음 |
| Passed이면서 Warnings > 0 | 0건 | 표본 내 반례 없음 |

특히 Vim IM Select는 `Warnings 30 + Other 2 = 32 issues`에서 Satisfactory이고, Evernote Decryptor는 `Warnings 31 + Other 2 = 33 issues`에서 Caution이다. 동일한 Other 2를 둔 인접 경계 쌍이므로 현재 공개 snapshot에서 warning count 30/31이 tier 경계라는 추론은 강하다. 다만 scanner/backend 산식은 공식 문서·공개 client code에서 발견되지 않았고 Scorecard 자체도 WIP이므로 영구 계약이나 미래 보장으로 승격하지 않는다(SRC-002, SRC-029).

운영상 결론은 명확하다. 현재 Achmage의 `scan Risks 0 / Warnings 50 / Other 2`에서 Satisfactory 관찰 구간으로 들어가려면 warning을 최소 20개 줄여 30 이하로 만들어야 한다. CSS 외 8개를 모두 제거해도 42라서 tier는 그대로일 것으로 강하게 예상된다. 반대로 CSS 42개를 기능·theme·접근성 회귀 없이 제거하면 나머지가 그대로여도 Warnings 8이 되어 관찰된 Satisfactory 구간에 충분한 여유로 들어간다. Passed를 노리려면 공개 표본상 Warnings 0이 필요하지만 Other 2를 점수 때문에 제거할 근거는 관찰되지 않았다.

## Findings

### F-001 — 225는 225개의 독립 보안 결함이 아니다

- **판정:** 사실/관찰.
- **근거:** SRC-001, SRC-009, SRC-010.
- **내용:** 공개 합계는 `Warnings 222 + Other 3`; Risks는 0이다. `Disclosures 6`과 `Passed 5`는 합계에서 제외된다. 193건은 assertion과 한 CSS cluster에서 나온 반복 occurrence다.
- **신뢰도:** 높음.
- **반례·제약:** scanner 내부에서는 occurrence별 가중치가 다를 수 있으며 공개 formula가 없다.
- **결정 영향:** “보안상 심각한 225개”라는 오해를 피하고 반복 패턴 단위로 작업한다.

### F-002 — 공개 산식은 발견되지 않았고 preview scan이 가장 직접적인 공식 사전 경로다

- **판정:** 사실 + 공개 표본에서의 추론.
- **근거:** SRC-002, SRC-004, SRC-006, SRC-014.
- **내용:** 2026-08-02에 검토한 공식 blog/docs와 공개 `obsidianmd` source에서는 Passed/Caution threshold를 발견하지 못했고 local lint와 scanner rule set도 다르다. 공식 dashboard는 branch/tag/commit preview scan을 제공한다. 현재 공개 `Caution`은 `Add to Obsidian`이 보이는 상태이고, 공식 문서도 이 summary label을 review failure와 동일하다고 정의하지 않는다.
- **신뢰도:** 높음.
- **반례·제약:** backend 정책이 이후 공개될 수 있다. Preview와 release scan의 완전한 동등성도 공식 문서에 보장되지 않았고 release asset/attestation 및 advisory DB 시점이 다를 수 있다.
- **결정 영향:** plan AC는 경고 수 예상 대신 exact candidate commit의 preview result를 필수 사전 근거로 포함하고, publish 후 public new-version scan을 최종 결과로 기록해야 한다.

### F-003 — assertion 151건은 가장 큰 zero-runtime 후보이나 vendor maintenance 경계가 있다

- **판정:** 사실 + 권고.
- **근거:** SRC-001, SRC-009, SRC-011.
- **내용:** 147건이 vendored Pretext, 4건이 own code다. 대부분 compile-time only라 runtime JS가 동일해야 한다. 단 iterator result처럼 안전한 구조로 바꿔야 하는 예외가 있고, vendor fork diff가 늘어난다.
- **신뢰도:** 높음.
- **반례·제약:** scanner가 source lines를 다르게 deduplicate할 수 있고 upstream vendor sync 시 conflict가 생긴다.
- **결정 영향:** runtime-erased assertion은 독립 commit으로 AST/type-aware 수정하고 production JS byte/hash parity를 요구한다. Iterator `done` 검사처럼 emitted JS가 필요한 예외는 별도 commit과 behavior test를 사용한다.

### F-004 — `!important` 42건은 한 의도적 compatibility workaround다

- **판정:** 사실/관찰.
- **근거:** SRC-001, SRC-009.
- **내용:** 모두 `styles.css` typography controls이며 주석은 community theme override를 막는 목적을 명시한다.
- **신뢰도:** 높음.
- **반례·제약:** 더 높은 specificity는 상대 theme의 `!important`를 이기지 못할 수 있다.
- **결정 영향:** 삭제가 아니라 selector contract 재설계와 theme visual matrix가 필요하다.

### F-005 — dependency warning은 실제로 갱신해야 하지만 public 8과 현재 audit는 같은 스냅샷이 아니다

- **판정:** 사실/관찰.
- **근거:** SRC-001, SRC-012, SRC-015.
- **내용:** release lock의 5개 package가 public scanner에 노출됐고 현재 advisory DB는 dev esbuild와 추가 xmldom advisory까지 본다. non-force transitive patch 후보가 존재한다.
- **신뢰도:** 중간~높음.
- **반례·제약:** advisory DB와 dependency resolutions는 빠르게 변한다. dry-run의 예상 외 node_modules mutation도 재현성을 흐렸다.
- **결정 영향:** lockfile을 명시적으로 갱신하고 clean install에서 prod/dev audit와 full render acceptance를 분리한다.

### F-006 — 170 warning + 1 Other는 아키텍처 없이 우선 제거 가능하다

- **판정:** 추론/권고.
- **근거:** SRC-009, SRC-011.
- **내용:** assertion 151, Promise 5, activeDocument 5, unsafe 7, receiver assertion 1, globalThis 1, unused function 1은 의미 변경을 최소화해 처리할 수 있다.
- **신뢰도:** 중간~높음.
- **반례·제약:** activeDocument/globalThis는 headless/popout compatibility, promise wrapper는 rejection 처리, dead helper chain은 새 unused 발생을 검증해야 한다.
- **결정 영향:** Phase A로 묶되 runtime-erased assertions, emitted iterator guard, event/type, vendored DOM compatibility를 작은 commit으로 나눈다.

### F-007 — `main.js`는 경계 초과이며 단순 dead-code 정리만으로는 robust budget을 만들지 못한다

- **판정:** 사실/실험.
- **근거:** SRC-008, SRC-010, SRC-013.
- **내용:** canonical current artifact는 5,248,205 bytes다(F-016). unused `transformTables`는 production에서 이미 tree-shake된다. browser build의 약 30KB 감소도 decimal 5MB 및 4.9MB budget을 충족하지 못한다. MathJax, Highlight.js, embedded assets가 큰 축이다.
- **신뢰도:** 높음(크기), 중간(향후 scanner threshold).
- **반례·제약:** dependency/minifier 버전에 따라 수 KB~수십 KB 변한다.
- **결정 영향:** CI byte budget과 bundle metafile diff를 도입하고, 기능 보존형 size tranche를 별도로 검증한다.

### F-008 — direct fs와 dependency `new Function`은 first-party 의도가 아니라 PostCSS/source-map 경로다

- **판정:** 사실/실험.
- **근거:** SRC-010, SRC-011, SRC-013.
- **내용:** production entry의 first-party non-test runtime source에는 Node fs import가 없고 test/build의 `node:fs`는 release graph 밖이다. release bundle의 fs/new Function은 transitive path다. browser-platform 탐색 실험은 두 token을 제거하고 fixture parity를 유지했다.
- **신뢰도:** 높음(provenance), 중간(채택 안전성).
- **반례·제약:** live Obsidian, custom CSS/source maps, export, Electron compatibility는 미검증이다.
- **결정 영향:** dependency patch보다 build target candidate를 먼저 live-smoke하되, 결과를 size 해법 전체로 과장하지 않는다.

### F-009 — own `eval` 제거는 Scorecard cosmetic cleanup이 아니라 core overflow 변경이다

- **판정:** 사실/권고.
- **근거:** SRC-009, SRC-011, R-002.
- **내용:** `auditLoop.ts:135`는 iframe의 실제 DOM overflow를 검사하는 닫힌 루프에 있다. 제거하려면 execution boundary와 timing을 재설계해야 한다.
- **신뢰도:** 높음.
- **반례·제약:** 같은 document에 typed function을 주입하지 않고도 직접 inspect하는 더 단순한 설계가 가능할 수 있다.
- **결정 영향:** 이번 Scorecard tranche에서 보류하고, 필요 시 독립 research/plan과 deterministic parity AC를 둔다.

### F-010 — network/base64/clipboard는 숨길 대상이 아니라 정확히 설명할 capability다

- **판정:** 사실/관찰/권고.
- **근거:** SRC-001, SRC-003, SRC-009, SRC-011.
- **내용:** public detail의 network 28 flagged locations는 로컬에서 KaTeX의 비-HTTP `.fetch()` 명칭 28곳에 매핑됐다. 명시적 first-party network API callsite는 export의 `requestUrl` 한 곳이지만 Tier 3 remote background와 Markdown external image는 preview iframe/browser가 load할 수 있다. clipboard는 explicit write-only buttons, base64는 self-contained export다.
- **신뢰도:** 높음.
- **반례·제약:** scanner가 다음 버전에서 semantic analysis를 개선하거나 disclosure UI를 변경할 수 있다.
- **결정 영향:** 기능을 제거하거나 우회 API로 숨기지 않고 README와 dashboard disclosure를 구체화한다.

### F-011 — unavailable scan disclosures는 저장소에서 해결할 수 있는 warning으로 보이지 않는다

- **판정:** 관찰.
- **근거:** SRC-001, SRC-002.
- **내용:** malware/dependency/obfuscation/network scan unavailable 항목에 author-side remediation이 공개되지 않았다.
- **신뢰도:** 중간.
- **반례·제약:** owner dashboard에 public page에 없는 재실행 버튼이나 상태가 있을 수 있다.
- **결정 영향:** 코드 우회를 하지 말고 preview/public scan에서 재확인한 뒤 platform issue라면 공식 채널에 제기한다.

### F-012 — release attestation pass는 보존해야 할 신뢰 자산이다

- **판정:** 사실/권고.
- **근거:** SRC-001, SRC-004, SRC-005, SRC-010.
- **내용:** main.js/styles.css attestation과 source build verification은 이미 Passed다. 새 release는 tag/manifest/version/assets가 일치하고 같은 commit의 successful governance/build/attestation을 사용해야 한다.
- **신뢰도:** 높음.
- **반례·제약:** Obsidian release requirements와 GitHub action versions는 변경될 수 있다.
- **결정 영향:** Scorecard cleanup 중 release workflow를 단순화하거나 attestation을 제거하지 않는다.

### F-013 — R-003의 bundle size 결과를 loading performance 결과로 해석할 수 없다

- **판정:** 사실/범위 결정.
- **근거:** SRC-013.
- **내용:** 이 조사는 bytes와 render parity를 측정했지만 startup timeline, CPU, memory, first preview latency를 측정하지 않았다.
- **신뢰도:** 높음.
- **반례·제약:** bundle 감소가 일부 환경에서 실제 load 개선과 상관될 가능성은 있다.
- **결정 영향:** R-004 후보에서 cold/warm 조건, vault size, first-render marker, memory, regression budget을 별도 정의한다.

### F-014 — UX와 Scorecard를 같은 release에 넣을 수 있지만 검증 tranche는 분리해야 한다

- **판정:** 추론/권고.
- **근거:** SRC-002, SRC-009, SRC-010; 관련 연구 R-002 및 F-002, F-003~F-013.
- **내용:** 최종 release는 하나여도 navigation UI, zero-runtime lint, dependency, CSS, build target은 failure signature가 다르다.
- **신뢰도:** 높음.
- **반례·제약:** release 일정이 매우 짧으면 tranche 수가 운영 부담이 될 수 있다.
- **결정 영향:** commit/plan AC를 tranche별로 나누고 각 단계에서 preview scan과 behavior tests를 비교 가능하게 남긴다.

### F-015 — 재현된 lock-only patch는 renderer major를 유지하면서 production audit를 0으로 만든다

- **판정:** 격리 실험.
- **근거:** SRC-016.
- **내용:** `1.0.3` lock에서 non-force lock-only audit fix는 Marp `4.3.0`과 Marpit `3.2.1`을 유지하면서 SRE/xmldom/js-yaml/linkify-it/markdown-it/postcss/nanoid를 patch·minor 범위로 갱신해 production audit를 6→0으로 만들었다. 남은 full-audit Moderate 1은 direct dev `esbuild@0.24.2`였고 별도 `0.28.1` 갱신으로 full audit도 0이 됐다. 원본의 stale lock root version `1.0.1`도 package `1.0.3`과 정렬됐다.
- **신뢰도:** 재현 snapshot은 높음; 향후 registry 해법은 중간.
- **반례·제약:** advisory DB와 semver resolution은 변동하며 dependency output/render parity는 실제 변경 tranche에서 다시 검증해야 한다.
- **결정 영향:** REC-004의 구현 후보를 exact graph로 좁히되 registry 결과가 달라지면 자동 수용하지 않는다. esbuild는 transitive patch와 분리된 build-tool commit으로 취급한다.

### F-016 — 1.0.3 committed `main.js`는 stale하지 않으며 raw checkout 크기 차이는 CRLF 변환이다

- **판정:** 재현 사실 및 기존 수치 해석 교정.
- **근거:** SRC-017.
- **내용:** tag blob, 공개 release asset, 격리 clean build는 모두 5,248,205 bytes와 SHA-256 `a283…12bd`로 일치한다. 5,254,896 bytes/SHA-256 `687b…d735`는 `core.autocrlf=true` checkout의 raw CRLF bytes이며 LF 정규화하면 canonical hash가 된다.
- **신뢰도:** 높음.
- **반례·제약:** 향후 build가 실제 content diff를 만들 수 있으므로 “CRLF일 것”이라고 가정해 diff를 무시해서는 안 된다.
- **결정 영향:** 계획에서 “stale committed artifact 동기화”를 별도 제품 변경으로 만들지 않는다. 대신 tag blob/clean build/release digest의 canonical equality와 `git diff --exit-code -- main.js`를 검증한다.

### F-017 — Node 24와 draft-only publish가 1.1.0의 현재 공식 release profile이다

- **판정:** 공식 현재 상태 + 제품 결정.
- **근거:** SRC-018, SRC-019, SRC-020.
- **내용:** Node 20은 2026-03-24 EOL이고 Node 24는 LTS다. Obsidian 공식 guide는 annotated version tag, attested 세 asset, draft release, 사람의 notes 검토 후 publish 흐름을 제시한다. 제품 소유자는 exact candidate에서 Risks 0, errors/fails 0, 1.0.3 대비 Review inventory 비악화를 blocker로 정했고 Caution label 잔존은 허용했다.
- **신뢰도:** 높음; Scorecard backend 판정은 변동 가능.
- **반례·제약:** official example의 action major와 Node version 자체는 프로젝트가 현재 지원 버전으로 갱신해야 하며 preview/public scanner 동등성은 보장되지 않는다.
- **결정 영향:** workflows를 Node 24로 통일하고 tag job은 draft만 생성한다. exact main SHA의 green governance/quality와 preview evidence 후 tag하고, draft asset/hash/attestation/notes를 검토한 뒤 수동 공개한다.

### F-018 — 누락된 native 1920 harness는 기존 부채이며 1.1.0 작업으로 가장해 복구할 수 없다

- **판정:** 재현 사실 및 범위 결정.
- **근거:** SRC-020, SRC-021, 관련 R-002/F-012.
- **내용:** `scripts/render-native-1920-v5-acceptance.mjs:37`은 존재하지 않는 `src/tests/renderNative1920V5Acceptance.ts`를 entry로 참조하고 2026-08-03 재실행도 esbuild resolve error/exit 1이었다. navigation/Scorecard 변경 때문에 새로 생긴 실패가 아니다.
- **신뢰도:** 높음.
- **반례·제약:** 누락 harness가 의도했던 coverage 일부는 다른 render corpus와 겹칠 수 있으나 동일하다는 증거는 없다.
- **결정 영향:** 기존 command를 삭제·이름 변경·가짜 PASS 처리하지 않는다. 1.1.0 gate에서는 새 navigation harness, overflow 36-case matrix와 명시된 render/export corpus를 사용하고 native harness 복구는 별도 R-ID로 남긴다. 기존 SC-AC-005의 해당 명령 PASS 문구는 이 부록으로 대체된다.

### F-019 — 1.1.0 preview는 비교 가능한 aggregate inventory와 잔여 ledger로 판정해야 한다

- **판정:** candidate 구현 후 decision revision.
- **근거:** SRC-001, SRC-020, SRC-022.
- **내용:** 공개 1.0.3과 branch preview의 category surface가 동형이 아니므로 개별 category 일대일 비교는 재현할 수 없다. Risks 0, errors/fails 0, warning 총수 `48 < 222`, warning category 수 `5 < 12`와 잔여 5-category ledger를 pre-release gate로 사용한다. Completion-only docs diff는 preview SHA의 scanner-relevant product tree와 동일해야 한다.
- **신뢰도:** aggregate inventory와 product-tree equality는 높음; preview/public surface equivalence는 낮음~중간.
- **반례·제약:** category 수 감소는 개별 rule count 감소와 같지 않으며 branch preview에 없는 release-only 구역은 publish 후 public scan에서만 판정할 수 있다.
- **결정 영향:** REC-018과 SC-AC-018이 REC-016/SC-AC-016의 1.1.0 적용 방식을 대체한다. publish 후 public 1.1.0 rescan은 유지한다.

### F-020 — pre-merge readiness와 post-tag draft evidence는 순서가 다른 두 단계 gate다

- **판정:** governance/release workflow 재현 후 decision revision.
- **근거:** SRC-019, SRC-020, SRC-023.
- **내용:** 저장소의 승인된 process와 Governance check는 complete E 뒤 PR merge를 요구한다. 현재 GitHub branch protection은 없어 owner가 실패 check를 우회할 수 있지만, 본 릴리스 절차에서는 우회하지 않는다. 같은 exact `main` SHA의 green run은 tag workflow가 actual three assets·attestation·draft를 만들기 위한 technical hard gate다. Actual draft를 E completion 선행 조건으로 두면 순환하므로 AC-212는 pre-merge contract를, tag workflow는 post-completion actual evidence를 담당한다.
- **신뢰도:** 높음; local complete-required validator와 실제 PR failure, workflow 조건을 함께 확인했다.
- **반례·제약:** pre-merge rehearsal은 실제 GitHub attestation 성공을 대신하지 않는다. Branch protection이 없으므로 merge discipline은 owner process에 의존하지만, post-tag workflow는 same-SHA main success가 없으면 실패한다. post-tag job 실패 시 draft 검토·공개를 중지해야 한다.
- **결정 영향:** REC-019와 SC-AC-019로 release 순서를 고정하고 어느 post-tag gate라도 실패하면 publish하지 않는다.

### F-021 — `1.1.2`의 Scorecard 개선은 정량적으로 성공했지만 같은 Caution tier 안에 머물렀다

- **판정:** 공개 post-release 사실/관찰.
- **근거:** SRC-001, SRC-022, SRC-025, SRC-028.
- **내용:** 공개 issues는 `225 → 52`(-76.9%), warnings는 `222 → 50`(-77.5%), warning category는 `12 → 7`로 줄었고 Risks는 계속 0이다. Dependency warning 8건은 사라져 `No vulnerable dependencies found`가 Passed에 추가됐으며 build reproduction도 pass다. 그럼에도 Review label과 막대는 각각 Caution, 2/4로 유지됐다.
- **신뢰도:** 높음; 동일 public surface의 두 release ledger를 비교했다.
- **반례·제약:** scanner rule과 advisory DB는 두 시점 사이에 변할 수 있으므로 172건 전부가 source cleanup만의 효과라고 분해할 수는 없다. 그러나 exact public totals와 현재 release 판정 자체는 직접 관찰값이다.
- **결정 영향:** 릴리스 계획의 “Risks/errors/fails 0과 baseline 대비 비악화, Caution만으로 차단하지 않음” gate는 의도대로 작동했다. 이번 결과를 개선 실패로 분류하지 않되, 다음 목표가 막대 상승이라면 별도의 tier-targeted gate가 필요하다.

### F-022 — Review 막대는 연속 진척률이 아니라 warning-count tier를 표현하는 categorical indicator다

- **판정:** 강한 경험적 추론; 공식 산식은 비공개.
- **근거:** SRC-002, SRC-025, SRC-026, SRC-029.
- **내용:** 공개 UI는 네 segment와 `Passed / Satisfactory / Caution / Risks` label을 함께 렌더한다. 결정론적 512-page 표본 중 509개 성공, scan Risks 0인 457개에서는 `Warnings 0 → Passed`, `1~30 → Satisfactory`, `31 이상 → Caution 또는 Risks`에 반례가 없었다. 동일 Other 2를 가진 Warnings 30/Satisfactory와 31/Caution의 인접 쌍도 관찰됐다. 따라서 225에서 52로 줄어도 두 값이 모두 Caution 구간이면 2/4 막대가 그대로인 것이 현재 UI 동작과 일치한다.
- **신뢰도:** 현재 snapshot의 경계 추론은 높음; 공식·미래 계약으로서의 신뢰도는 낮음~중간.
- **반례·제약:** 표본은 전체 6,314개의 census가 아니며 3개 parsing failure가 있다. scan Risks가 있는 경우 별도 규칙이 섞이고, scanner가 갱신되면 경계도 바뀔 수 있다. 공개 공식 자료와 client bundle에서는 산식 자체를 확인하지 못했다.
- **결정 영향:** 다음 candidate에서 Satisfactory를 목표로 하면 `Warnings ≤ 30`을 관찰 기반 최소 조건으로, scanner drift 여유를 둔 `≤ 28`을 운영 target으로 사용하고 preview/public scan으로 최종 판정한다. 이를 “공식 threshold”라고 문서화하지 않는다.

### F-023 — 남은 `!important` 42건이 막대 상승의 결정적 병목이다

- **판정:** 공개 count + local provenance + 과거 theme 실험의 결합 판정.
- **근거:** SRC-025, SRC-026, SRC-028, SRC-030 및 기존 F-004/REC-005.
- **내용:** CSS cluster는 현재 warnings 50의 84%다. 비-CSS 8건을 전부 제거해도 `50 → 42`로, 관찰된 30/31 경계의 Caution 쪽에 남는다. 이전 specificity 후보는 scanner상 42개를 제거했지만 theme matrix에서 track contrast `0/21`, thumb `3/21`, focus `0/21`, Settings active 검증 불완전으로 기각됐다. 현재 CSS 유지 결정은 점수보다 실제 가시성과 접근성을 우선한 결과다.
- **신뢰도:** 현재 병목과 과거 기각 근거는 높음; 새 CSS 설계의 성공 가능성은 미측정.
- **반례·제약:** 공식 산식이 바뀌거나 rule별 weight가 도입되면 warning count만으로 tier를 예측할 수 없다. 일부 CSS만 제거하는 접근은 기존 all-or-rollback tranche의 검증 경계를 다시 설계해야 한다.
- **결정 영향:** 막대 상승이 다음 릴리스의 명시 목표라면 저위험 source hygiene보다 먼저 “theme-safe CSS specificity 재설계”를 독립 tranche로 연구·계획한다. raw `!important` 삭제나 scanner 우회는 금지하고 Default light/dark 및 Obsidianite·Minimal·AnuPpuccin에서 track/thumb/active/focus/keyboard를 통과한 경우만 채택한다.

### F-024 — 비-CSS 8건은 품질상 정리 가치가 있지만 단독으로는 Review 막대를 올리지 못한다

- **판정:** source mapping과 경계 산술.
- **근거:** SRC-025~SRC-028.
- **내용:** dev-only console/unsafe 3건은 production bundle에서 tree-shake되므로 테스트로 옮기면 runtime artifact를 바꾸지 않을 수 있다. `createElement` 2건은 captured document의 `doc.win.createEl/createSpan`로 바꿀 후보이나 measurement·emoji·popout 회귀 검증이 필요하다. Declarative settings 1건은 Obsidian 1.13+ search 개선이지만 minAppVersion 1.7.2를 보존하려면 21개 이상의 imperative/custom control과 dual support를 유지해야 한다. Size 1건과 Direct FS 1건은 각각 bundle composition과 browser-platform/live runtime gate가 필요한 별도 tranche다. 이 8건을 모두 없애도 Warnings 42다.
- **신뢰도:** provenance와 산술은 높음; 각 후보 구현의 실제 scan 결과는 미측정.
- **반례·제약:** 수정 과정에서 새 warning이 생길 수 있고 declarative API/type package가 계속 변할 수 있다. `createElement`는 vendored Pretext core measurement path라 단순 문자열 치환 대상으로 보면 안 된다.
- **결정 영향:** 이 항목들은 설정 검색, source hygiene, bundle portability 같은 자체 제품 가치가 있을 때 실행한다. 막대 한 칸을 약속하는 단독 score tranche로 포장하지 않는다.

### F-025 — 현재 Other 2건을 제거하는 것은 Review tier 개선의 필요조건으로 관찰되지 않았다

- **판정:** 공개 비교 관찰 + 기능 경계 유지 결정.
- **근거:** SRC-010, SRC-025, SRC-026 및 기존 F-009/F-010.
- **내용:** 표본에서는 Warnings 0, Other 9인 Full Calendar Remastered가 Passed였고, Warnings 30/31의 인접 경계도 같은 Other 2에서 갈렸다. 현재 자료에서는 Other count가 Review tier 경계를 직접 움직였다는 반례를 찾지 못했다. Achmage의 Clipboard Access는 explicit user action이고 Dynamic Code Execution의 first-party `eval`은 deterministic overflow audit 경로다.
- **신뢰도:** 현재 표본 관찰은 중간~높음; backend formula 비공개로 인과 확정은 불가.
- **반례·제약:** 특정 Other subtype에 별도 weight가 있을 가능성을 공개 자료만으로 완전히 배제할 수 없다. 보안 검토는 점수와 독립적으로 계속 필요하다.
- **결정 영향:** Passed label을 위해 clipboard/export/overflow 기능을 삭제하지 않는다. Other는 정확히 disclosure하고 실제 보안·제품 근거가 있을 때만 별도 연구로 변경한다.

### F-026 — Theme-native slider 결정

- **근거:** SRC-028, SRC-030, SRC-032.
- **내용:** 세 slider를 Obsidian `SliderComponent` grammar로 통일하고 plugin CSS는 flex/min-width 같은 layout만 소유한다. Settings 두 component에는 test용 stable class만 추가하고 Preview raw range는 기존 limits·instant update·value/save/reset/focus 계약을 보존한 component로 교체한다.
- **사실:** 기준선의 Settings `Base font size`와 `Wash opacity`는 Obsidian `SliderComponent`를 사용하지만 Preview quick typography는 raw `<input type="range">`를 사용한다. `styles.css`의 range/reset 제어에는 declaration-level `!important` 42건이 남아 있다. 이전 specificity 후보는 warning 42건을 제거했지만 theme matrix에서 track/thumb/focus 대비와 Settings active-state 검증을 충족하지 못해 전량 rollback되었다.
- **관찰:** 자체 track/thumb 색과 cascade를 다시 소유하면 community theme별 시각 상태를 계속 재현해야 한다. 반대로 세 control을 Obsidian component grammar로 통일하고 plugin CSS를 배치에만 한정하면 활성 theme가 track, thumb, focus와 active 외형을 소유한다.
- **결정:** Settings의 두 `SliderComponent`는 유지하고 test용 안정 class만 추가한다. Preview raw range는 `SliderComponent`로 바꾸되 기존 limits, instant update, 값 동기화, 저장, Reset, 숫자 입력과 focus 흐름을 보존한다. Range/reset의 강제 시각 CSS와 `!important`는 전부 제거하고 flex/min-width 등 layout만 plugin이 소유한다. Shadow DOM, custom slider, inline important, selector 반복이나 CSS layer를 이용한 scanner 우회는 사용하지 않는다.
- **반증·중단 조건:** Default light/dark, Minimal 9.0.2, AnuPpuccin 1.5.0, Obsidianite 2.0.2 중 하나라도 track/thumb/focus 3:1, clipping, keyboard/mouse round-trip, Settings pop-out 또는 Preview 재개방 검증에 실패하면 slider/CSS tranche 전체를 rollback하고 `1.1.3` release를 중단한다.
- **반례·제약:** Native component 사용 자체가 모든 community theme의 3:1 대비나 active-state를 보장하지 않는다. 고정 theme matrix와 OS-level Settings drag가 구현 후 반증 gate다.
- **결정 영향:** Shadow DOM/custom slider/scanner 우회 없이 range/reset의 forced visual rules와 declaration-level `!important` 42건을 한 rollback 단위로 제거한다.
- **신뢰도:** 중간. Component ownership과 이전 실패 원인은 직접 확인했지만 최종 candidate의 고정 theme matrix는 구현 후 검증 대상이다.

### F-027 — UTF-8 bundle 실험과 판정

- **근거:** SRC-028, SRC-031, SRC-032.
- **내용:** 동일 node-platform source/dependency graph에서 esbuild `charset: "utf8"`만 적용하면 production output이 5,268,064에서 5,095,067 bytes로 172,997 bytes(3.28%) 감소한다. Runtime parity와 deterministic encoding gate를 통과할 때 기능보존형 배포 크기 감소로 채택한다.
- **사실:** 기준선 production `main.js`는 5,268,064 bytes다. 동일 source와 dependency graph를 사용한 in-memory esbuild 비교에서 기본 ASCII escape 출력은 5,268,064 bytes, `charset: "utf8"` 출력은 5,095,067 bytes였다. 차이는 `-172,997 bytes` 또는 `-3.28%`다.
- **관찰:** 이 차이는 문자열을 ASCII escape로 직렬화하는 방식과 UTF-8 code point로 직렬화하는 방식의 배포 표현 차이다. `platform: "node"`와 dependency graph를 유지하므로 Direct Filesystem의 transitive provenance나 runtime module selection을 제거하는 실험이 아니다.
- **결정:** `1.1.3` 후보는 esbuild에 `charset: "utf8"`만 추가하고 `platform: "node"` 및 dependency graph를 유지한다. 이 변경은 acceptance parity와 deterministic clean-build hash를 통과할 때만 **기능 보존형 배포 크기 감소**로 채택한다.
- **주장 경계:** byte 감소를 plugin loading 또는 first Preview 성능 개선으로 표현하지 않는다. 또한 public scanner의 `main.js > 5 MB` warning 제거를 보장하지 않는다. Warning이 남더라도 candidate가 Satisfactory gate를 충족하면 알려진 잔여로 기록한다. 기존 REC-006의 `<4,900,000 bytes`는 scanner 경계에 충분한 여유를 둔 별도 size-decision 목표로 보존하며, 이번 UTF-8 tranche의 기능보존 budget과 동일시하지 않는다.
- **반증·중단 조건:** 두 clean build의 hash가 다르거나 UTF-8 validity, BOM/replacement character, CJK·emoji·math·code·HTML export parity, `<5,150,000 bytes` 또는 `<5 MiB` gate 중 하나라도 실패하면 이 tranche를 채택하지 않는다.
- **반례·제약:** Bytes 감소는 load-time/first-preview 개선의 증거가 아니며 scanner가 size warning을 제거할지도 미확정이다. `platform: "node"`라 Direct Filesystem provenance도 남는다.
- **결정 영향:** Build config에는 charset만 추가하고 release notes에는 distribution size 감소만 기술한다. 성능 개선·size-warning 제거를 주장하지 않는다.
- **신뢰도:** in-memory byte 결과는 높음. Runtime parity와 public size-warning 영향은 구현 전이므로 중간 이하이며 별도 gate가 필요하다.

### F-028 — 1.1.3 범위 고정

- **근거:** SRC-027, SRC-031, SRC-032.
- **내용:** 이번 patch의 예상 source residual은 declarative settings 1건이며 public release에서는 Direct Filesystem과 size warning 가능성을 별도 reconcile한다. 설정 dual implementation과 browser-platform/source-map 실험은 독립적인 호환성 범위이므로 제외한다.
- **사실:** declarative settings warning을 없애려면 minAppVersion `1.7.2` 지원과 Obsidian 1.13+ API를 함께 보존하는 dual implementation, settings search와 persistence 검증이 필요하다. Direct Filesystem warning은 기준 dependency graph의 transitive PostCSS 경로에서 발생하며 browser-platform build는 module resolution과 source-map 동작 범위를 바꾼다.
- **결정:** `1.1.3`에서는 declarative settings 전환, Direct Filesystem 제거, browser-platform build와 source-map stub을 제외한다. 설정 schema·저장 상태와 node-platform dependency graph를 보존하며, 해당 warning은 숨기거나 우회하지 않고 예상 잔여 inventory에 기록한다.
- **반증·계획 변경 조건:** candidate Dashboard에 declarative settings 1건 외 예상하지 않은 source warning이 나타나거나 목표 Review가 `Satisfactory`가 아니면 merge/tag/release를 중단한다. 새 해법을 임의 적용하지 않고 R-003과 연결 계획을 먼저 갱신한다.
- **반례·제약:** Scanner backend와 public release-only category는 변동 가능하고 preview가 public 결과를 완전히 보장하지 않는다.
- **결정 영향:** Exact candidate preview가 Risks 0, errors/fails 0, Satisfactory와 source warning 1건을 만족하지 않으면 merge/tag/release하지 않는다. Declarative settings·Direct FS·browser build를 즉석에서 추가하지 않는다.
- **신뢰도:** 높음(승인된 scope 결정). Scanner backend와 public release-only category는 변동 가능하다.

### F-029 — Captured-window helper는 type-only Window augmentation이 필요하다

- **판정:** 구현 전 local type reproduction.
- **근거:** SRC-033.
- **내용:** `doc.win.createEl("canvas")`와 `doc.win.createSpan()`은 실제 Obsidian helper ownership에는 맞지만 installed `obsidian` types는 `Node.win`을 일반 `Window`로 선언하고 helper를 global function으로만 노출한다. 두 호출을 그대로 적용한 exact compile은 TS2339 두 건으로 실패했고, 기존 `src/types/dom-augment.d.ts`의 `Window`에 `createEl: typeof createEl`, `createSpan: typeof createSpan`을 type-only로 추가하면 full project typecheck가 0 diagnostics로 통과했다.
- **신뢰도:** 높음. 현재 lock의 `obsidian` declaration과 exact candidate source를 TypeScript compiler로 재현했다.
- **반례·제약:** 향후 Obsidian types가 helpers를 `Window`에 직접 선언하면 augmentation이 중복될 수 있으므로 dependency 갱신 시 재확인한다. 선언은 runtime polyfill이 아니므로 helper가 없는 foreign window를 새로 지원하지 않는다.
- **결정 영향:** Pretext helper tranche에는 기존 DOM augmentation 파일의 두 type-only member를 포함하고 runtime receiver cast를 사용하지 않는다. Captured-window ownership과 typecheck/overflow/popout parity를 함께 검증한다.

## Alternatives considered

| 대안 | 장점 | 단점·위험 | 필요한 검증 | 판정 |
|---|---|---|---|---|
| A. 모든 label을 즉시 0으로 만들기 | 표면상 최대 점수 가능성 | export/clipboard/audit/math/language 기능 삭제, core overflow 위험 | 사실상 전 제품 회귀 검증 | 기각 |
| B. 문서 disclosure만 보강 | 제품 동작 위험 최소 | Warnings 222가 남아 Caution 인상 개선 가능성 낮음 | public page/dash disclosure | 단독 해법 기각, 보조로 채택 |
| C. Phase A+B만 적용 | 약 179건 감소 가능, 아키텍처 영향 매우 작음 | CSS/size/fs가 남고 status 불확실 | type/build parity, clean audit, full acceptance, preview | 권장 최소선 |
| D. Phase A+B+C+browser build + 별도의 기능보존 size 해법 | 모든 조건이 충족되면 이론상 Other 2개 수준까지 접근 | browser build만으로 size warning은 남으며 추가 해법은 아직 없음; CSS/build runtime risk | theme matrix, live Obsidian, size gate, preview | 조건부 목표선, 아직 실증되지 않음 |
| E. Highlight language를 26개로 제한 | 약 1MB 감소, robust size budget | rare language highlighting 기능 퇴행 | supported-language policy, fixtures, release notes | 조건부; 별도 제품 결정 |
| F. font/background 별도 asset화 | main.js 크게 감소 가능 | 현재 3-asset 배포 contract와 offline self-contained 성격 변경 | packaging/install/sync/export 전면 검증 | R-003 범위 밖 |
| G. Marp fork/custom build | 의존성·크기를 정밀 제어 | 유지보수 비용과 renderer divergence 큼 | 장기 upstream sync 전략 | 기각 |
| H. own eval 제거 | Dynamic Other 감소 가능 | deterministic overflow audit 핵심 경로 변경 | 별도 research + timing/DOM parity | 보류 |
| I. 경고 이름만 우회·난독화 | scanner count가 줄 수도 있음 | 정책·신뢰·유지보수 악화, 실제 위험 은폐 | 정당화 불가 | 금지 |

## Impact analysis

### 제품과 UX

- Phase A는 사용자 기능을 바꾸지 않는 것이 목표다. promise wrapper의 save/render 순서, typed arrays, active document 선택만 보존하면 체감 변화가 없어야 한다.
- CSS tranche는 typography slider의 track/thumb/active/focus 가시성에 직접 영향을 준다. 특히 community theme에서 퇴행할 수 있다.
- clipboard, remote image export, base64를 제거하면 명백한 UX/기능 손실이므로 유지한다.
- Highlight language 제한은 “경량화”이지만 code fence 지원 범위를 줄이는 제품 결정이다.
- R-002 navigation UI는 자체 discoverability 목표와 interaction acceptance가 있으므로 Scorecard 수정과 같은 commit에 뒤섞지 않는다.

### 아키텍처와 데이터

- 타입/assertion/dead code/CSS specificity/lock refresh는 data schema나 slide parsing model을 바꾸지 않는다.
- browser-platform build는 bundle resolution boundary를 바꾸므로 build architecture에는 영향이 있지만 runtime feature architecture를 재설계하지는 않는다.
- asset 외부화, math engine 제거, Marp fork, own eval bridge 재설계는 현 범위의 “아키텍처 비변경”을 넘어선다.
- 저장된 설정 schema, vault markdown, exported HTML contract는 모든 권장 tranche에서 보존되어야 한다.

### 성능과 자원 사용

- 작은 main.js는 disk/sync/download와 parse 비용에 긍정적일 수 있으나 실제 latency 개선은 미측정이다.
- browser-platform 후보의 약 30KB 감소는 성능 개선으로 해석할 정도의 근거가 없다.
- Highlight.js 축소는 약 1MB로 유의미하지만 feature compatibility cost가 있다.
- embedded font/background는 약 1.18MB source payload지만, 별도 asset화가 load/memory를 개선하는지는 R-004에서 측정해야 한다.

### 보안과 개인정보

- known dependency advisories는 실제로 제거해야 하며, prod와 dev dependency를 구분해 audit한다.
- network destination은 bundled default R2 URL, 사용자 Tier 3 override, Markdown external image를 포함한다. Preview에서는 iframe/browser가 resource를 load할 수 있고 export에서는 `requestUrl`이 fetch/inline한다. 각 trigger와 data flow를 문서화한다.
- clipboard는 read가 아니라 user-triggered write다.
- direct fs warning은 dependency의 source-map path이며 first-party local override는 Obsidian `FileSystemAdapter`를 통해 desktop-only 기능으로 공개돼 있다.
- eval은 same-origin presentation iframe audit에 쓰이지만 동적 실행 특성 자체는 맞다. 입력이 어떻게 구성되는지와 외부 untrusted string이 섞이지 않는지를 구현 계획에서 재확인해야 한다.

### 호환성과 마이그레이션

- activeDocument 변경은 popout에는 유리할 수 있으나 `typeof document` guard와 실제 `doc` 선택이 불일치하면 headless/OffscreenCanvas fallback을 깨뜨릴 수 있다.
- Marp transitive patches는 Markdown/PostCSS/parser 결과를 바꿀 수 있다. lock refresh 후 all fixtures와 exported HTML을 비교한다.
- CSS specificity는 Chromium/WebKit pseudo-elements와 Firefox pseudo-elements 모두 유지해야 한다.
- browser build는 desktop-only manifest, Electron runtime, Obsidian external APIs, Node fallback이 필요한지 live test한다.
- data migration은 없어야 한다. settings JSON diff가 생기면 범위 이탈이다.

### 테스트와 관측 가능성

- official lint에 project type info와 `prefer-active-doc` overlay를 추가한다.
- runtime-erased assertion tranche는 production `main.js` SHA-256/byte equality를 가장 강한 no-runtime-change 증거로 쓴다.
- dependency/build tranche는 bundle metafile group diff와 token provenance를 보존한다.
- UI는 default light/dark, 최소 3개 specificity가 강한 community theme, hover/active/focus/keyboard를 캡처한다.
- overflow matrix, native 1920, bidi/CJK/emoji/font-stress, bento/card, wide/long table, both MathJax/KaTeX를 유지한다.
- popout, narrow pane, fullscreen, Tier 3/Markdown external resource preview, remote image export, offline self-contained HTML을 포함한다.
- candidate commit preview scan의 전체 category count와 public release scan을 execution record에 보존한다.

### 빌드, 배포, 문서

- `main.js`는 committed release artifact이며 build 후 diff를 검토한다.
- `< 4,900,000 bytes`를 기능보존 size 후보의 decision target으로 삼고, 그 해법을 채택할 때만 CI hard budget으로 둔다. 달성하지 못하면 기능을 몰래 제거하지 말고 residual warning과 후속 결정을 기록한다.
- release workflow의 source verification과 main.js/styles.css attestation을 보존한다.
- manifest `x.y.z`, release tag, release assets가 일치해야 한다.
- README의 기존 Tier 3/export network disclosure를 실제 trigger/destination과 대조해 정밀화하고, base64 purpose, clipboard write, FileSystemAdapter scope를 보강한다.
- optional `CONTRIBUTING.md`는 Health hygiene 개선이지만 Review 작업과 별도 commit으로 둔다.

## Contradictions, uncertainties, and open questions

| ID | 내용 | 결론에 미치는 영향 | 해소 방법 | 해당 단계 |
|---|---|---|---|---|
| Q-001 | 검토한 공식 자료에서 Review status 산식과 threshold를 찾지 못함 | Passed/Caution 변화 보장 불가 | candidate preview scan + public rescan | 모든 단계/릴리스 전 |
| Q-002 | Scorecard의 5 MB가 decimal인지 binary인지 비공개 | 경계 최적화의 성공 불확실 | `<4,900,000` budget + preview scan | build tranche |
| Q-003 | official lint에서 `prefer-active-doc`은 off이나 Scorecard는 flag | local green이 scan green과 불일치 | overlay + dashboard scan | Phase A |
| Q-004 | public advisory 8의 exact unique advisory mapping 미공개 | “8 CVE” 주장 불가 | lock audit + dashboard result 저장 | Phase B |
| Q-005 | advisory DB가 release 이후 변함 | 결과 재현 시 count가 다를 수 있음 | scan date, lock SHA, audit JSON 기록 | Phase B/릴리스 직전 |
| Q-006 | `npm audit fix --dry-run`이 node_modules를 실제 변경 | ambient install 측정 오염 | `npm ci` clean install, tracked lock 권위 | 구현 시작 전 |
| Q-007 | browser-platform fixture parity는 live Electron parity가 아님 | build 후보 채택 불가 | 실제 Obsidian startup/preview/export/popout smoke | Phase C/D |
| Q-008 | CSS 상대 theme도 `!important` 사용 가능 | 42개 제거 시 slider 회귀 | theme matrix; 실패 시 일부 warning 유지 결정 | CSS tranche |
| Q-009 | unavailable scan이 owner dashboard에서 재실행 가능한지 불명 | author action 판단 불확실 | owner dashboard 확인; 필요 시 #plugin-dev | release gate |
| Q-010 | Other가 남아도 Passed인 표본이 있지만 Achmage도 같은지 불명 | clipboard/dynamic 제거 필요성 불확실 | Phase C 후 preview scan | 조건부 Phase D |
| Q-011 | own eval의 input trust boundary를 이번 조사에서 data-flow 증명하지 않음 | 보안 평가 잔여 | 별도 eval research에서 string construction audit | 후속 R-ID |
| Q-012 | bundle 감소와 actual loading speed 상관 미측정 | 성능 개선 주장 불가 | R-004 cold/warm benchmark | 별도 연구 |
| Q-013 | Highlight rare languages의 실제 사용량 데이터 없음 | 26-language 제한 채택 근거 부족 | 지원 정책/사용자 조사 또는 전면 지원 유지 | 별도 제품 결정 |
| Q-014 | Scorecard는 빠르게 진화 중 | 보고서 유효기간 짧음 | 2026-09-01 이전 규칙 재확인; 릴리스 직전 refresh | 계획 승인/릴리스 전 |
| Q-015 | Dashboard preview가 release assets·attestation·동시점 advisory까지 public scan과 완전히 같은지는 문서화되지 않음 | preview PASS만으로 final status 보장 불가 | version/build 확정 SHA preview + tag 동일성 + publish 후 public rescan | release gate |
| Q-016 | 509개 공개 표본에서 scan Risks 0의 warning tier 경계가 0/1 및 30/31로 깨끗했지만 공식 산식은 공개되지 않음 | `≤30`을 영구 계약으로 쓰면 future scanner drift에 취약 | 운영 target `≤28`, exact candidate preview, publish 후 public rescan | 다음 score-targeted tranche |
| Q-017 | Review label `Risks`와 scan의 Risks section count는 같은 개념이 아니며 scan Risks 0에서도 Review Risks 표본 5개가 관찰됨 | label만 보고 실제 risk count를 역추론하면 오류 | label, Risks, Warnings, Other를 각각 보존 | 모든 후속 비교 |

## Recommendations

### REC-001 — 목표를 “Passed 보장”이 아니라 “저위험 최대 제거 + 실제 preview 판정”으로 정의한다

- **근거 Finding:** F-001, F-002, F-014.
- **권고:** 계획 성공 기준에 Risks 0 유지, known warnings 최대 제거, deliberate remaining capability rationale, candidate commit preview scan, release scan 기록을 둔다. 공개 산식 추정값을 AC로 쓰지 않는다.
- **채택 조건:** owner dashboard에서 branch/commit preview가 가능함을 확인한다.
- **기각/롤백 조건:** preview 결과를 execution record로 보존할 수 없으면 release를 보류한다.

### REC-002 — Phase A를 네 개의 독립 low-risk commit으로 실행한다

- **근거 Finding:** F-003, F-006.
- **권고:** (A1a) runtime-erased assertions/receiver cast, (A1b) `PretextMeasurementEngine.ts:238` iterator 안전화, (A2) Promise·unsafe types·dead helper, (A3) activeDocument/globalThis로 나눈다. 예상 제거는 warning 170 + Other 1이다.
- **채택 조건:** official type-aware lint, typecheck, acceptance가 통과하고 A1a는 production JS가 byte/hash equivalent다. A1b는 예상한 iterator guard 외 bundle 차이가 없고 cache eviction behavior가 동일하다.
- **기각/롤백 조건:** A1a bundle이 달라지거나 A1b cache behavior 또는 A3 headless/OffscreenCanvas/popout 결과가 달라지면 해당 commit을 분리 재조사한다.

### REC-003 — vendored Pretext 경고는 AST/type-aware 방식으로만 정리한다

- **근거 Finding:** F-003.
- **권고:** regex 일괄 치환을 금지하고 rule autofix diff를 review한다. upstream/vendor provenance와 수정 이유를 주석 또는 vendor sync 기록에 남긴다.
- **채택 조건:** all Pretext unit/overflow/bidi/font fixtures, runtime-erased subset의 bundle byte parity, emitted iterator refactor의 설명 가능한 diff.
- **기각/롤백 조건:** assertion 제거가 inference를 `any`로 약화하거나 upstream sync 비용이 과도하면 first-party 4건만 먼저 처리하고 잔여를 정당화한다.

### REC-004 — dependency lock을 의도적으로 갱신하고 clean environment에서 이중 audit한다

- **근거 Finding:** F-005.
- **권고:** Marp major/fork 없이 현재 declared range 안의 transitive patch를 먼저 시도한다. `npm audit --omit=dev`와 전체 `npm audit`를 분리하고 esbuild major는 별도 build compatibility commit으로 다룬다.
- **채택 조건:** clean `npm ci`, prod advisory 0 목표, typecheck, overflow/native/export acceptance, preview dependency warning 소거.
- **기각/롤백 조건:** renderer output hash/geometry가 달라지고 원인을 수용할 근거가 없으면 lock update를 좁혀 재시도한다.

### REC-005 — `!important`는 selector specificity 재설계와 theme matrix가 준비된 경우에만 제거한다

- **근거 Finding:** F-004.
- **권고:** owner-scoped dedicated classes로 reset/range track/thumb/active/focus를 재작성한다. 42개를 한 CSS component test 단위로 취급한다.
- **채택 조건:** default light/dark + 최소 3개 강한 community theme + keyboard focus/active screenshots PASS.
- **기각/롤백 조건:** theme에서 white-on-white/track loss/focus loss가 한 건이라도 재현되고 warning 일부를 유지하는 것보다 안전한 selector가 없으면 해당 `!important`를 유지하고 rationale를 기록한다.

### REC-006 — size gate는 scanner 경계보다 충분한 headroom을 둔다

- **근거 Finding:** F-007.
- **권고:** `< 4,900,000 bytes`를 기능보존 size tranche의 목표 budget/decision gate로 둔다. 달성 가능한 해법이 승인된 경우에만 CI hard gate로 전환하고 bundle metafile의 group별 증감을 기록한다.
- **채택 조건:** 기능 보존 후보로 target을 달성하고 all acceptance와 preview size warning을 통과한다.
- **기각/롤백 조건:** math engine, rare language, embedded offline asset 기능을 조용히 삭제해야만 달성되면 size warning을 명시적 residual로 남기고 별도 제품 결정/R-004로 올린다.

### REC-007 — browser-platform build를 fs/new Function 제거의 첫 실험 후보로 삼되 바로 채택하지 않는다

- **근거 Finding:** F-007, F-008.
- **권고:** dependency 내부 patch보다 esbuild platform/browser resolution 후보를 먼저 clean lock 환경에서 재현한다. fixture hash 외 live Obsidian, custom CSS/source maps, export, popout을 검증한다.
- **채택 조건:** `require("fs")`와 dependency `new Function` 0, live/fixture parity, external resolution 정상, size budget을 다른 기능 보존 최적화와 함께 달성.
- **기각/롤백 조건:** Electron runtime error, custom CSS/source-map behavior 차이, Obsidian API resolution 문제 발생.

### REC-008 — Highlight language 축소는 Scorecard 수정으로 몰래 포함하지 않는다

- **근거 Finding:** F-007.
- **권고:** robust size margin이 다른 방법으로 불가능할 때만 supported-language policy로 별도 결정한다. rare languages를 plaintext fallback으로 바꾸면 release notes와 tests에 명시한다.
- **채택 조건:** 제품 소유자가 호환성 축소를 명시 승인하고 representative language matrix를 정의한다.
- **기각/롤백 조건:** 기존 full syntax highlighting이 제품 약속이거나 사용 데이터 없이 축소를 정당화할 수 없음.

### REC-009 — own eval은 이번 tranche에서 유지한다

- **근거 Finding:** F-009.
- **권고:** Dynamic Code Execution Other 하나를 줄이기 위해 deterministic audit을 건드리지 않는다. Phase C 후 preview status가 목표에 못 미치고 eval이 결정적 원인으로 확인될 때 별도 research를 만든다.
- **채택 조건:** current input construction과 same-origin boundary를 문서화하고 untrusted code injection이 없음을 후속 검토한다.
- **기각/롤백 조건:** 보안 audit에서 실제 injection path가 발견되면 우선순위를 즉시 올린다.

### REC-010 — capability는 구체적으로 disclosure하고 scanner를 game하지 않는다

- **근거 Finding:** F-010, F-011.
- **권고:** README/dashboard의 기존 문구를 검증·보강하여 export `requestUrl`뿐 아니라 Tier 3 R2/user URL 및 Markdown external resource의 preview browser load까지 destination/trigger/data flow로 적고, clipboard write-only action, base64 self-contained export, FileSystemAdapter local override를 설명한다. KaTeX 28-location mapping 근거를 execution record에 보존한다.
- **채택 조건:** 문서가 실제 코드와 일치함을 source review로 확인한다.
- **기각/롤백 조건:** disclosure 때문에 기능을 숨기거나 custom encoder/API alias를 도입하려는 변경은 금지한다.

### REC-011 — local scorecard preflight를 두 층으로 만든다

- **근거 Finding:** F-002, F-006.
- **권고:** official `eslint-plugin-obsidianmd@0.4.1` pinned config + project typing + `prefer-active-doc` overlay를 도입하고, 별도 CSS `!important`, exact byte size, `npm audit`, bundle token checks를 추가한다. 이것이 actual Scorecard parity가 아님을 문서화한다.
- **채택 조건:** CI 시간이 수용 가능하고 generated/vendor 포함 범위를 실제 scanner와 대조한다.
- **기각/롤백 조건:** local green을 공식 scan green으로 표시하는 오해가 생기면 명칭을 `scorecard-preflight`로 제한한다.

### REC-012 — version/build가 확정된 동일 candidate commit을 preview하고 publish 후 재판정한다

- **근거 Finding:** F-002, F-012, F-014.
- **권고:** version과 committed `main.js`/manifest/styles를 candidate commit에 먼저 확정한다. 그 exact SHA를 preview scan하고 tag가 같은 SHA를 가리키는지 확인한 뒤, tag checkout에서 draft release assets를 build/attest해 publish한다. 공개 후 Scorecard가 새 version을 가리키는지와 preview 차이를 확인한다. Preview가 release-only asset verification과 완전히 동등하다는 가정은 하지 않는다.
- **채택 조건:** preview에 errors/fail flags가 없고 target review 상태 또는 명시 승인된 잔여가 기록되며, publish 후 public scan이 별도로 확인됨.
- **기각/롤백 조건:** tag와 manifest 불일치, asset가 다른 commit에서 생성, attestation 실패, 공개 scan 악화.

### REC-013 — Health hygiene는 Review와 분리해 선택적으로 개선한다

- **근거 Finding:** F-001.
- **권고:** `CONTRIBUTING.md` 추가는 저비용이며 Health hygiene를 완성할 수 있지만 Review 225 문제 해결로 계산하지 않는다.
- **채택 조건:** 실제 contribution/issue/reporting 절차를 담을 수 있음.
- **기각/롤백 조건:** 빈 형식 문서만 만들어 score를 game하게 되는 경우.

### REC-014 — 실제 로딩 속도와 경량화는 별도 R-004 후보로 조사한다

- **근거 Finding:** F-013.
- **권고:** cold plugin enable, warm reload, first Open Slide Preview, first stable render, large document, memory peak/steady state, bundle parse/eval profile를 정의한다. R-003의 metafile은 가설 생성 자료로만 사용한다.
- **채택 조건:** 성능 업데이트를 실제 계획 범위에 넣기로 결정한 경우 새 R-ID를 생성한다.
- **기각/롤백 조건:** bytes 감소만으로 “loading faster”를 선언하는 계획은 허용하지 않는다.

### REC-015 — 재현된 dependency graph를 두 tranche로 적용하고 registry drift를 gate한다

- **근거 Finding:** F-005, F-015.
- **권고:** 첫 tranche는 `npm audit fix --package-lock-only --ignore-scripts --no-fund`로 재현된 SRE/xmldom/js-yaml/linkify-it/markdown-it/postcss/nanoid graph와 lock root version 정렬만 적용하고 Marp/Marpit을 유지한다. 두 번째 tranche는 direct dev `esbuild@0.28.1`만 다룬다. 각 tranche 전후 exact lock tree와 prod/full audit JSON을 저장한다.
- **채택 조건:** clean install에서 expected graph, prod/full audit 0, typecheck/build/render/export acceptance가 모두 통과하고 package.json의 의도하지 않은 runtime range 변화가 없어야 한다.
- **기각/롤백 조건:** registry가 다른 graph를 제안하거나 renderer output·geometry가 달라지면 변경을 강행하지 않고 R-003과 계획을 먼저 revision한다.

### REC-016 — canonical artifact·Node 24·draft Scorecard gate를 하나의 release invariant로 고정한다

- **근거 Finding:** F-002, F-012, F-016, F-017.
- **권고:** release workflow를 Node `24.x`와 draft-only `gh release create --draft`로 갱신하고 attestation을 유지한다. tag/HEAD Git blob, clean tag build, draft `main.js` digest가 같은 canonical LF bytes인지 확인한다. exact candidate preview는 Risks 0, errors/fails 0, 1.0.3 대비 전체 및 개별 Review category 비악화를 요구하되 `Caution` label만으로 차단하지 않는다.
- **채택 조건:** 같은 main SHA의 governance/quality success, annotated no-`v` tag와 manifest version 일치, 세 release asset hash/attestation, 사람이 검토한 한·영 notes와 수동 publish를 확인한다.
- **기각/롤백 조건:** content diff를 CRLF로 오인해 무시함, EOL Node 사용, auto-publish, tag/manifest/SHA 불일치, attestation 실패, preview blocker 악화 중 하나라도 발생하면 publish하지 않는다.

### REC-017 — missing native harness를 숨기지 않고 별도 연구 부채로 유지한다

- **근거 Finding:** F-018, 관련 R-002/F-012.
- **권고:** `acceptance:native-1920-v5`를 복구·삭제·재사용하지 않는다. 실행 기록에는 baseline부터 missing source로 실패한다는 사실을 남기고, 1.1.0 검증은 새 navigation browser matrix, 기존 overflow matrix, 명시적 render/export corpus를 각각 실행한다.
- **채택 조건:** 대체 gate가 자신이 검증하는 범위를 정확히 명명하고 native coverage 복구를 주장하지 않아야 한다.
- **기각/롤백 조건:** 1.1.0 변경이 native harness의 고유 coverage를 실제로 요구한다면 제품 수정을 멈추고 별도 R-ID와 계획을 먼저 만든다.

## Planning handoff

| Research / Finding | 계획에 반영할 결정 | 예상 변경 표면 | 필수 검증 | 미해결 전제 |
|---|---|---|---|---|
| R-003 / F-001,F-002 | 공개 count 대신 exact candidate preview를 사전 gate, public new-version scan을 최종 판정으로 사용 | CI, execution record, release process | candidate SHA scan + publish 후 public rescan | rating formula 및 preview/release equivalence 비공개 |
| R-003 / F-003,F-006 | Phase A low-risk source cleanup | listed TS/vendor files | official lint, typecheck, A1a JS byte/hash parity, A1b cache behavior, all acceptance | vendor sync policy |
| R-003 / F-004 | CSS specificity rewrite를 독립 tranche로 실행 | `styles.css`, control class creation sites | 5+ theme/mode matrix, focus/active screenshots | third-party `!important` |
| R-003 / F-005 | clean transitive lock patch 먼저 | `package-lock.json` 중심, 필요 시 package.json | clean ci, prod/full audit, render/export parity | advisory DB drift |
| R-003 / F-007 | `<4,900,000`을 기능보존 size decision gate로 사용하고 해법 채택 시 CI hard budget화 | esbuild config/CI, size artifact | production size, metafile diff, preview warning | 기능 보존 최적화 충분성; 미달 시 residual 승인 |
| R-003 / F-008 | browser-platform 후보를 build experiment로 검증 | `esbuild.config.mjs` 및 externals | fs/new Function token 0, 79×2 fixture, live Obsidian/export/popout | Electron compatibility |
| R-003 / F-009 | own eval 현 tranche 보류 | no change; 후속 research candidate | input trust audit, existing overflow tests | status에서의 실제 영향 |
| R-003 / F-010,F-011 | capability 유지 및 disclosure 강화 | README/dashboard metadata | source↔disclosure audit, public page 확인 | dashboard UI/scan availability |
| R-003 / F-012,F-014 | 관련 연구 R-002의 UX와 scorecard를 한 release로 통합하되 commits/AC 분리 | navigation UI + scorecard tranches + release | R-002 UX AC, R-003 AC, same-SHA attestation | release 일정 |
| R-003 / F-013 | loading performance는 별도 research | R-004 후보 | cold/warm latency, memory, first render | 새 연구 승인 |
| R-003 / F-015 | 재현된 runtime dependency patch와 esbuild toolchain patch를 분리 | package lock 및 dev dependency declaration | exact tree, clean ci, prod/full audit 0, render/export parity | registry/advisory drift 시 연구 revision |
| R-003 / F-016 | stale artifact 수정 대신 canonical LF artifact equality를 검증 | build/release verification, execution record | tag blob = clean build = release digest; normalized Git diff 0 | Windows raw CRLF hash는 release digest가 아님 |
| R-003 / F-017 | Node 24, exact candidate Scorecard blocker, draft-only 수동 publish | CI/release workflow, release process | same-SHA checks, Risks 0, errors/fails 0, inventory 비악화, asset attestation | Caution label 잔존 허용; publish 후 public rescan |
| R-003 / F-018 | missing native harness는 별도 부채로 유지하고 새 navigation/overflow/render gates 사용 | test orchestration과 execution record만; 누락 command/source 비변경 | baseline failure 기록, 각 대체 gate의 명시적 coverage | 복구는 별도 R-ID |

### 다음 계획에서 안정적인 AC로 전환할 후보

1. **SC-AC-001:** candidate commit의 공개 inventory가 execution record와 산술 일치한다.
2. **SC-AC-002:** official type-aware lint + `prefer-active-doc` overlay가 0 error/warning 또는 승인된 explicit exception만 남긴다.
3. **SC-AC-003:** runtime-erased assertion tranche 전후 동일 clean dependency graph의 production `main.js`는 byte-identical이다. Iterator `done` 검사 tranche는 별도 commit이며 예상 guard 외 차이가 없고 cache eviction test가 PASS한다.
4. **SC-AC-004:** `npm ci`, `npm audit --omit=dev`, full `npm audit` 결과가 각각 저장되고 prod advisories 0을 목표로 한다.
5. **SC-AC-005:** `npm run typecheck`, `npm run acceptance:all`, `npm run acceptance:native-1920-v5`가 PASS한다.
6. **SC-AC-006:** bidi/CJK/emoji/font-stress, bento/card, wide/long table, MathJax/KaTeX render parity가 유지된다.
7. **SC-AC-007:** typography control이 default light/dark와 최소 3개 강한 community theme에서 track/thumb/active/focus 모두 보인다.
8. **SC-AC-008 (조건부):** 기능보존 size tranche를 채택할 경우 production `main.js`가 `< 4,900,000 bytes`이고 CI가 초과를 실패시킨다. 달성할 안전한 해법이 없으면 size warning을 residual risk로 명시 승인하고 별도 연구/제품 결정으로 이관한다.
9. **SC-AC-009:** build 후보 채택 시 release bundle에 dependency 유래 `require("fs")`와 `new Function`이 없고 first-party eval 잔여가 의도적으로 기록된다.
10. **SC-AC-010:** Tier 3 R2/user URL과 Markdown external image의 preview load, remote image export, offline self-contained HTML, local override image, clipboard copy가 disclosure와 일치하며 정상이다.
11. **SC-AC-011:** version과 committed artifacts가 확정된 exact candidate commit의 preview scan에 errors/fails가 없고, Risks 0 및 목표 Review 또는 승인된 잔여 결과가 기록된다.
12. **SC-AC-012:** manifest version, exact Git tag, committed main.js/manifest/styles.css, source commit, tag checkout에서 만든 release assets와 artifact attestations가 동일 release를 가리킨다.
13. **SC-AC-013:** 공개 release 후 public Scorecard가 새 version을 가리키고, preview와의 차이를 포함한 최종 결과가 기록된다.
14. **SC-AC-014:** `npm run governance:check -- --base HEAD`가 PASS하고 `main.js` generated diff가 검토된다.

### 2026-08-03 AC addendum

기존 SC-AC-005의 `acceptance:native-1920-v5` PASS 요구는 baseline부터 충족 불가능하며 F-018/REC-017로 대체한다. 원문을 삭제하지 않고 다음 안정 ID로 1.1.0 계획에 넘긴다.

15. **SC-AC-015:** isolated clean install의 exact dependency tree가 승인된 graph와 일치하고 production/full audit가 각각 0이며 dependency와 esbuild tranche의 render/export parity가 통과한다.
16. **SC-AC-016:** Node 24 workflow가 exact candidate의 Risks 0, errors/fails 0, 1.0.3 대비 전체·개별 category 비악화를 확인하고 draft만 생성한다. tag blob, clean tag build, draft asset의 canonical SHA-256이 동일하고 attestation·수동 notes 검토 후 공개한다. `Caution` label 잔존만으로는 실패시키지 않는다.
17. **SC-AC-017:** missing native source와 baseline exit 1을 실행 기록에 보존하고 해당 command를 고치거나 green으로 표시하지 않는다. 새 navigation matrix, overflow 36-case matrix, 승인된 render/export corpus가 각각 PASS한다.

### 2026-08-04 release-candidate decision revision

REC-016과 SC-AC-016의 원문은 당시 목표로 보존한다. 실제 1.1.0 candidate scan과 governance/release workflow의 실행 순서를 대조한 결과, 다음 두 결정이 기존 문구를 1.1.0에 적용하는 방식으로 대체한다.

#### Evidence detail — F-019

- **근거:** SRC-001, SRC-020, SRC-022.
- **사실:** 공개 1.0.3은 Warnings 222와 12개 warning category를 표시했다. exact product candidate `1d8fd58`의 authenticated preview는 Risks 0, errors/fails 0, 48 warning locations와 5개 category를 표시했다. candidate 잔여는 environment access 1, console assertion 2, declarative settings 1, vendored create-element 2, CSS `!important` 42로 합계 48이다. Branch preview에는 release-only Releases/Behavior 구역이 없었다.
- **관찰:** 두 UI가 노출하는 category 집합과 구역이 동형이 아니어서, 공개 baseline의 각 category를 candidate category에 일대일 대응해 “개별 category count 비악화”를 계산할 수 없다. category 수와 warning 총수는 각각 `5 < 12`, `48 < 222`로 비교 가능하다.
- **결정:** 1.1.0 pre-release blocker는 사용자 승인 원문대로 Risks 0, errors/fails 0, warning 총수 비악화, warning category 수 비악화로 판정한다. 잔여 5개 category와 count를 숨기지 않는다. publish 후 같은 공개 surface에서 1.1.0을 다시 스캔해 preview 차이를 최종 판정한다.
- **정확한 candidate 경계:** `1d8fd58` 뒤 completion commit은 R/P/E/Register만 바꾸고 scanner-relevant product/source/dependency/workflow tree는 바꾸지 않는다. 따라서 preview SHA와 tag 전 product candidate tree의 동일성을 `git diff --exit-code 1d8fd58 -- <relevant paths>`로 확인한다. 해당 경계가 깨지면 AC-211을 재실행한다.
- **신뢰도:** aggregate inventory는 높음, preview와 public surface equivalence는 낮음~중간.

#### Sequence detail — F-020

- **근거:** SRC-019, SRC-020, SRC-023.
- **사실:** PR Governance check는 changed product/operation paths에 complete E-ID를 요구하지만 현재 branch protection은 없어 owner merge의 technical hard block은 아니다. 승인된 release process는 이 check를 우회하지 않는다. Release workflow는 annotated tag 전에 같은 `main` SHA의 successful push Governance run을 hard gate로 요구한다. 실제 세 assets, attestation과 draft release는 tag push 뒤에만 생성된다.
- **관찰:** 실제 draft asset을 E-002 completion의 선행 증거로 요구하면 `complete E → merge/main success → tag → draft` 순서와 순환한다.
- **결정:** AC-212는 pre-merge release-readiness gate로 한정한다. version tuple, deterministic artifact, Node 24 quality, complete-required governance, workflow syntax와 exact three-asset/attestation/draft-only contract를 검증한다. merge 후 exact main SHA의 green run을 확인한 뒤 annotated tag를 push하고, 실제 workflow의 asset hash·attestation·draft 결과를 외부 release evidence로 확인한다. 실패하면 publish하지 않고 tag/asset을 덮어쓰지 않는다.
- **신뢰도:** 높음; repository workflow와 실제 Governance failure/success 조건으로 재현.

### REC-018 — 1.1.0 Scorecard blocker는 비교 가능한 aggregate inventory로 고정한다

- **근거 Finding:** F-019.
- **권고:** Risks 0, errors/fails 0, warning 총수와 warning category 수의 1.0.3 대비 비악화, 잔여 category ledger 공개를 pre-release gate로 사용한다. completion-only documentation diff 뒤에는 exact product-tree equality를 확인한다.
- **기각/롤백 조건:** scanner-relevant path가 바뀌거나 aggregate가 악화되면 새 preview 없이 merge/tag하지 않는다.

### REC-019 — release readiness와 실제 deployment evidence를 순서대로 분리한다

- **근거 Finding:** F-020.
- **권고:** E-002/AC-212는 merge-ready contract를 완료하고, 실제 main green run·annotated tag·세 assets·attestation·draft는 tag workflow가 순서대로 생성·검증하게 한다. draft 검토 전 공개하지 않는다.
- **기각/롤백 조건:** same-SHA main run, tag identity, asset set/hash, attestation, draft 상태 중 하나라도 실패하면 publish를 중지한다.

### REC-020 — 다음 막대 목표는 `Satisfactory`와 Warnings `≤28`로 명시한다

- **근거 Finding:** F-021, F-022.
- **권고:** 다음 Scorecard-focused candidate의 운영 target은 scan Risks 0, errors/fails 0, Warnings `≤28`로 둔다. 공개 표본의 30/31 경계보다 2건 여유를 두는 값이며, 공식 threshold라고 부르지 않는다. Candidate preview와 publish 후 public page에서 label, four-segment bar, Risks/Warnings/Other를 모두 다시 기록한다.
- **채택 조건:** scanner-relevant exact candidate SHA가 고정되고 preview 결과를 보존할 수 있음.
- **기각/롤백 조건:** scanner rule이 바뀌거나 Warnings가 28을 넘으면 등급을 약속하지 않고 R-003/P 계획을 먼저 갱신한다.

### REC-021 — 막대를 실제로 움직이려면 CSS specificity를 별도 theme-safe tranche로 다시 설계한다

- **근거 Finding:** F-023.
- **권고:** `styles.css:155-287`의 reset 2건과 range slider 40건을 단순 삭제하지 말고 wrapper scope, selector ownership, Obsidian component API/CSS variables와 cascade 순서를 다시 설계한다. 42건 전체를 한 tranche에서 다루되, Default light/dark·Obsidianite·Minimal·AnuPpuccin의 settings/popover에서 track, thumb, active, focus, keyboard, contrast가 모두 통과해야 채택한다. 성공하면 Warnings가 현재 다른 항목 그대로여도 8로 내려가 관찰된 Satisfactory 구간에 충분한 여유가 생긴다.
- **채택 조건:** public/preview preflight에서 `!important` warning 0, theme matrix 전 항목 PASS, visual snapshots와 owner smoke 보존.
- **기각/롤백 조건:** 어느 theme/상태에서든 가시성·대비·keyboard focus가 퇴행하면 tranche 전체를 되돌리고 Caution을 수용한다. 부분 제거를 원하면 검증 경계를 새 계획에서 다시 정의한다.

### REC-022 — 비-CSS 8건은 막대가 아니라 자체 품질 이익으로 우선순위를 정한다

- **근거 Finding:** F-024.
- **권고:** dev assertion 3건은 production byte parity가 유지될 때, document-scoped create helper 2건은 overflow/emoji/popout parity가 통과할 때, declarative settings 1건은 Obsidian 1.13+와 1.7.2 dual support의 settings/search/persistence가 모두 통과할 때만 채택한다. Size와 Direct FS는 기존 R-004/build research gate를 유지한다.
- **채택 조건:** 각 tranche가 독립적인 사용자·유지보수 이익과 regression test를 가짐.
- **기각/롤백 조건:** “8건을 줄이면 막대가 오른다”만이 근거라면 실행하지 않는다. 전부 제거해도 현재 관찰상 Warnings 42/Caution이다.

### REC-023 — Clipboard와 Dynamic Code Execution은 점수 때문에 제거하지 않는다

- **근거 Finding:** F-025.
- **권고:** Other 2건은 README disclosure와 실제 user-trigger/security boundary를 유지한다. Export/clipboard/overflow audit 동작은 별도 제품·보안 연구가 변경 필요성을 입증할 때만 다룬다.
- **채택 조건:** capability 설명과 실제 call path가 일치함.
- **기각/롤백 조건:** scanner label을 숨기기 위한 alias, indirection, 기능 삭제는 금지한다.

18. **SC-AC-018:** exact product candidate preview의 Risks 0, errors/fails 0, warning 총수와 warning category 수가 공개 1.0.3보다 비악화이고 잔여 5개 category ledger가 기록된다. completion-only docs diff는 scanner-relevant product tree equality로 묶는다.
19. **SC-AC-019:** pre-merge에는 version/artifact/governance와 draft-only workflow contract가 통과한다. merge 후에는 exact main green run을 확인한 annotated tag workflow가 실제 three-asset hash, attestation과 draft 상태를 검증하며, 사람이 검토하기 전 공개하지 않는다.
20. **SC-AC-020:** 공개 `1.1.2` post-release ledger가 `Risks 0 / Warnings 50 / Other 2 / issues 52 / Caution 2-of-4`로 기록되고 공개 `1.0.3` 대비 delta와 candidate preview 48 대비 release-only 2건이 reconcile된다.
21. **SC-AC-021:** 다음 Satisfactory-targeted 계획을 승인할 경우 exact candidate preview는 scan Risks 0, errors/fails 0, Warnings `≤28`이고 제거한 각 warning tranche의 기능·theme 회귀 gate가 PASS다.
22. **SC-AC-022:** 해당 release publish 후 public page가 exact version을 가리키며 Review label/bar와 Risks/Warnings/Other가 기록된다. 관찰 경계와 다르면 공식 산식으로 가정하지 않고 R-003을 다시 갱신한다.

### REC-024 — 세 slider를 Obsidian theme-native component로 통일한다

- **근거 Finding:** F-023, F-026.
- **권고:** Preview raw range를 `SliderComponent`로 전환하고 Settings 두 slider와 함께 stable test class, accessible name, 기존 limits/value/save/reset 계약을 보존한다. Plugin CSS는 layout만 담당하고 range/reset의 declaration-level `!important`와 browser-specific forced visual styling을 0으로 만든다.
- **채택 조건:** 고정 Obsidian/theme matrix의 idle·hover·keyboard focus·pointer active 상태, 3:1 visibility, mouse/keyboard round-trip, pop-out/reopen와 settings persistence가 전부 PASS한다.
- **기각/롤백 조건:** 한 theme나 control이라도 실패하면 전체 slider/CSS tranche를 되돌리고 release를 중단한다.

### REC-025 — UTF-8 charset을 독립적인 기능보존 build tranche로 적용한다

- **근거 Finding:** F-027.
- **권고:** esbuild에 `charset: "utf8"`만 추가하고 platform과 dependency graph는 고정한다. 두 clean production build의 hash/size와 CJK·emoji·math·code·export parity를 검증한다.
- **채택 조건:** deterministic valid UTF-8 output, no BOM/U+FFFD, `<5,150,000 bytes`와 `<5 MiB`, 전체 기능 parity를 모두 만족한다.
- **기각/롤백 조건:** runtime/export 결과가 달라지거나 결정론·size budget이 깨지면 rollback한다. 성능 개선이나 public size-warning 제거를 근거 없이 주장하지 않는다.

### REC-026 — declarative settings와 Direct Filesystem은 예상 잔여로 고정한다

- **근거 Finding:** F-028.
- **권고:** 이번 patch에서 settings dual implementation과 browser-platform/Direct Filesystem 실험을 하지 않는다. Exact candidate Dashboard는 Risks 0, errors/fails 0, 예상 source warning인 declarative settings 1건만 허용하며 public release-only Direct Filesystem과 size warning 가능성은 별도로 reconcile한다.
- **채택 조건:** Dashboard preview가 목표 `Satisfactory`를 표시하고 예상 inventory와 일치한다.
- **기각/롤백 조건:** 예상하지 않은 warning 또는 `Caution`이 나오면 tag를 만들지 않고 R-003과 계획을 먼저 revision한다.

### REC-027 — Obsidian helper typing은 기존 Window augmentation으로만 연결한다

- **근거 Finding:** F-024, F-029.
- **권고:** `doc.win.createEl/createSpan` 적용과 같은 tranche에서 `src/types/dom-augment.d.ts`의 `Window`에 두 helper를 `typeof`로 선언한다. Runtime cast, global document fallback, polyfill은 추가하지 않는다.
- **채택 조건:** Full typecheck 0 diagnostics, emitted runtime에는 augmentation 코드 0 bytes, captured document/popout/emoji/cache acceptance PASS.
- **기각/롤백 조건:** Runtime helper ownership이 실제 window와 다르거나 augmentation이 다른 declaration과 충돌하면 helper tranche를 rollback하고 R-003을 재검토한다.

## Source register

| Source ID | 유형 | 출처 | 버전·기준일 | 발행·수정일 | 접근일 | 사용 범위 | 신뢰도 |
|---|---|---|---|---|---|---|---|
| SRC-001 | web/primary UI | [Achmage Slides Ultra Community Scorecard](https://community.obsidian.md/plugins/achmage-slides-ultra) | latest release 1.0.3 | plugin update 2026-07-01; Scorecard WIP | 2026-08-02 | 전체 public Review ledger, counts, details | 높음(스냅샷), formula는 표시되지 않음 |
| SRC-002 | web/official | [The future of Obsidian plugins](https://obsidian.md/blog/future-of-plugins/) | automated review launch | 2026-05-12 | 2026-08-02 | every-version scan, preview scan, false positives, release failure 영향 | 높음 |
| SRC-003 | web/official | [Plugin security](https://obsidian.md/help/plugin-security) | current help | 접근 시점 current | 2026-08-02 | broad plugin permissions, automated/manual review | 높음 |
| SRC-004 | web/official docs | [Submit your plugin](https://docs.obsidian.md/Plugins/Releasing/Submit%20your%20plugin) | current developer docs | source latest 2026-05-12 | 2026-08-02 | manifest/tag/assets/new release feedback flow | 높음 |
| SRC-005 | web/official docs | [Release your plugin with GitHub Actions](https://github.com/obsidianmd/obsidian-developer-docs/blob/main/en/Plugins/Releasing/Release%20your%20plugin%20with%20GitHub%20Actions.md) | current guide | 2026-07-29 | 2026-08-02 | actions/attest permissions and release pattern | 높음 |
| SRC-006 | web/official source | [obsidianmd/eslint-plugin](https://github.com/obsidianmd/eslint-plugin), [`package.json` at 867ce74](https://github.com/obsidianmd/eslint-plugin/blob/867ce74/package.json), [`lib/index.ts` at 99b52f1](https://github.com/obsidianmd/eslint-plugin/blob/99b52f1/lib/index.ts), [typescript-eslint 8.35.1 recommended-type-checked](https://github.com/typescript-eslint/typescript-eslint/blob/v8.35.1/packages/eslint-plugin/src/configs/flat/recommended-type-checked.ts) | `eslint-plugin-obsidianmd` 0.4.1; TS ESLint 8.35.1 | package 2026-07-02; config 2026-06-30 | 2026-08-02 | local rule scope, type-aware inherited rules, recommended config gaps | 높음 |
| SRC-007 | web/official policy | [Developer policies](https://github.com/obsidianmd/obsidian-developer-docs/blob/main/en/Developer%20policies.md), [Plugin guidelines](https://github.com/obsidianmd/obsidian-developer-docs/blob/main/en/Plugins/Releasing/Plugin%20guidelines.md) | current | 2026-07-30 / 2025-06-23 | 2026-08-02 | disclosure, dependency, obfuscation, code quality constraints | 높음 |
| SRC-008 | web/official help | [Sync settings](https://obsidian.md/help/sync/settings), [Plans and storage limits](https://obsidian.md/help/sync/plans) | current | 접근 시점 current | 2026-08-02 | Standard 5 MB / Plus 200 MB official wording | 높음 |
| SRC-009 | local/source | baseline source paths: `src/**/*.ts`, `styles.css`, `README.md` | commit `b061ce8...`, working product source unchanged | 2026-08-02 baseline | 2026-08-02 | path:line mapping, intention comments, capability ownership | 높음 |
| SRC-010 | local/build | `package.json:30-38`, `package-lock.json:648,808,836,851,964`, `esbuild.config.mjs:17-47`, `.github/workflows/release.yml`, `main.js` | package 1.0.3, canonical Git blob 5,248,205 bytes; Windows CRLF checkout 5,254,896 bytes | baseline | 2026-08-02 | dependency graph, build/release behavior, bundled tokens | 높음 |
| SRC-011 | local/experiment | `rg` provenance audit, `npm run typecheck`, source line review | baseline | experiment 2026-08-02 | 2026-08-02 | warning locations, network/base64/clipboard/fs/eval provenance | 높음 |
| SRC-012 | local/live registry | `npm audit --json`, `npm outdated --json`, `npm view`, `npm ls --all` | npm advisory/registry snapshot | 2026-08-02 | 2026-08-02 | current dependency risk and patch candidates | 중간~높음; 변동성 큼 |
| SRC-013 | local/experiment | 본문의 esbuild `write:false` option delta, 79 fixtures × 2 engines hash protocol, 26-language resolver protocol | original lock environment | experiment 2026-08-02 | 2026-08-02 | bundle composition and candidate build behavior | 중간~낮음; inline script/output artifact 미보존, live UI 미검증 |
| SRC-014 | web/primary UI observation | [Frontmatter Date Manager](https://community.obsidian.md/plugins/frontmatter-date-manager), [Loft](https://community.obsidian.md/plugins/loft), [OpenLoops Hidden Files](https://community.obsidian.md/plugins/openloops-hidden-files), [Recent Files](https://community.obsidian.md/plugins/recent-files-obsidian), [Markdown Table Editor](https://community.obsidian.md/plugins/markdown-table-editor), [Personal OS](https://community.obsidian.md/plugins/personal-os) | public scorecards | 접근 시점 snapshot | 2026-08-02 | rating/count 비교; formula 추론만 | 중간 |
| SRC-015 | web/primary advisory | [GHSA-qx2v-qp2m-jg93](https://github.com/advisories/GHSA-qx2v-qp2m-jg93) | CVE-2026-41305, reviewed | published 2026-04-20; reviewed/updated 2026-04-24 | 2026-08-02 | public Scorecard가 링크한 PostCSS advisory | 높음 |
| SRC-016 | local/isolated experiment | tag `1.0.3`의 `package.json`·`package-lock.json`을 임시 archive에 추출; `npm audit --omit=dev --json`, `npm audit --json`, `npm audit fix --package-lock-only --ignore-scripts --no-fund --json`, `npm ls --package-lock-only --all`; 별도 `esbuild@0.28.1` lock-only 실험 | Node 24.12.0, npm 11.6.2; baseline/proposed exact graph | 2026-08-03 | 2026-08-03 | audit counts, exact resolved versions/path, package.json byte parity, lock root version, esbuild 분리 | 높음(snapshot), registry 변동성 큼 |
| SRC-017 | local build + GitHub release API | `git show 1.0.3:main.js`, `git ls-files --eol main.js`, raw/LF-normalized SHA-256, tag `1.0.3` 격리 `npm ci && npm run build`; [`1.0.3` release assets API/페이지](https://github.com/laguna821/achmage-slides-ultra/releases/tag/1.0.3) | tag/blob/release/clean build와 Windows checkout 비교 | release 2026-07-01; 재현 2026-08-03 | 2026-08-03 | canonical size/hash, CRLF working-tree 차이, committed artifact stale 가설 반증 | 높음 |
| SRC-018 | web/official | [Node.js Releases](https://nodejs.org/en/about/previous-releases) | v20 EOL, v24 LTS | v20 EOL 2026-03-24; living status page | 2026-08-03 | release workflow runtime 선택 | 높음, 상태 변동 가능 |
| SRC-019 | web/official docs | [Release your plugin with GitHub Actions](https://github.com/obsidianmd/obsidian-developer-docs/blob/08d3c94a00e3327c51bd967437452e0a3ece54ff/en/Plugins/Releasing/Release%20your%20plugin%20with%20GitHub%20Actions.md) | commit `08d3c94a00e3327c51bd967437452e0a3ece54ff` | 2026-07-29 | 2026-08-03 | annotated version tag, attestations, three assets, `--draft`, manual notes/publish | 높음 |
| SRC-020 | stakeholder decision | 제품 소유자가 승인한 Achmage Slides Ultra 1.1.0 통합 릴리스 계획과 2026-08-03 구현 요청 | release blocker 및 범위 확정 | 2026-08-03 | 2026-08-03 | Risks/errors/fails/inventory gate, Caution 허용, missing native harness 범위 분리 | 높음(결정) |
| SRC-021 | local/reproduction | `scripts/render-native-1920-v5-acceptance.mjs:35-39`, absent `src/tests/renderNative1920V5Acceptance.ts`, `npm.cmd run acceptance:native-1920-v5` | current source lineage; baseline부터 missing | 재현 2026-08-03 | 2026-08-03 | unresolved entry source와 esbuild exit 1 | 높음 |
| SRC-022 | authenticated primary UI observation | Obsidian Developer Dashboard preview for commit `1d8fd58b000169d001147d86d8841857ff373c50` | 1.1.0 exact product candidate | scan 2026-08-03 | 2026-08-03 | Risks/errors/fails, 48 locations, 5 category ledger, branch-preview surface limitation | 높음(snapshot), backend 변동 가능 |
| SRC-023 | local/GitHub workflow reproduction | `.github/workflows/governance.yml:64-65`, `.github/workflows/release.yml:35-430`, PR run `30780291644`, repository branch-protection/ruleset query | Node 24, complete-required PR check, same-main-SHA tag gate, assets/attestation/draft steps | 실행·재검토 2026-08-04 | 2026-08-04 | process-level merge discipline과 tag 후 draft/attestation의 technical hard-gate 순서 | 높음 |
| SRC-024 | stakeholder decision | repository owner의 2026-08-04 owner hallway 결과 및 “섹션이랑 슬라이드 구분해서 동작이 다 되니까 릴리즈 단계로 넘어가도 된다” 승인 | 1.1.0 release candidate | 2026-08-04 | 2026-08-04 | AC-206 navigation sign-off와 release 단계 진행 승인 | 높음(결정) |
| SRC-025 | web/primary UI | [Achmage Slides Ultra Community Scorecard](https://community.obsidian.md/plugins/achmage-slides-ultra) Scorecard tab server-rendered DOM | public latest `1.1.2`; Health Excellent, Review Caution 2/4, issues 52, Risks 0, Warnings 50, Other 2, Passed 6, Disclosures 5 | public scan current 2026-08-04 | 2026-08-04 | exact post-release ledger, warning titles/counts/source links, version metadata | 높음(snapshot), backend 변동 가능 |
| SRC-026 | web/official registry + primary UI sample | [`community-plugins.json` registry commit `3ffa4260`](https://github.com/obsidianmd/obsidian-releases/commit/3ffa42609e951897258669e13847271a015fada4); boundary examples [Vim IM Select](https://community.obsidian.md/plugins/vim-im-select), [Evernote Decryptor](https://community.obsidian.md/plugins/evernote-decryptor), [Plotly](https://community.obsidian.md/plugins/obsidian-plotly), [Finnish Spellcheck](https://community.obsidian.md/plugins/finnish-spellcheck), [Memoria](https://community.obsidian.md/plugins/memoria), [Full Calendar Remastered](https://community.obsidian.md/plugins/full-calendar-remastered) | 6,314 registry entries; equidistant 512 sample, 509 parsed, scan Risks 0 subset 457 | registry/page snapshot 2026-08-04 | 2026-08-04 | Review tier cross-table와 warning 0/1·30/31 empirical boundary; Other 비교 | 중간~높음(snapshot); 표본·비공식 산식 추론 |
| SRC-027 | web/official docs | [Migrate to declarative settings](https://docs.obsidian.md/plugins/guides/migrate-declarative-settings) | Obsidian 1.13.0+ `getSettingDefinitions()`; Path B dual support | 접근 시점 current/insider guidance | 2026-08-04 | current settings warning 의미, minAppVersion 1.7.2 호환 경로와 sync 위험 | 높음, API 상태 변동 가능 |
| SRC-028 | local/tag/release evidence | `git show 1.1.2:src/engine/slideRenderer.ts`, `src/vendor/pretext/measurement.ts`, `src/settings.ts`, `styles.css`, `main.js`; [GitHub release `1.1.2`](https://github.com/laguna821/achmage-slides-ultra/releases/tag/1.1.2) | tag dereference/main `e49b5028...`; `main.js` 5,268,064 bytes, SHA-256 `87b0f0f6...51c2`; local product tree equals tag | release published 2026-08-04 | 2026-08-04 | residual warning provenance, artifact size, exact release identity | 높음 |
| SRC-029 | web/server HTML + public client inspection | tier examples [Passed](https://community.obsidian.md/plugins/frontmatter-date-manager), [Satisfactory](https://community.obsidian.md/plugins/obsidian-style-settings), [Caution](https://community.obsidian.md/plugins/achmage-slides-ultra), [Risks](https://community.obsidian.md/plugins/dataview); [official WIP/false-positive notice](https://obsidian.md/blog/future-of-plugins/) | current Community frontend/server payload | 2026-08-04 snapshot; official blog 2026-05-12 | 2026-08-04 | fixed tier-to-segment mapping 4/4·3/4·2/4·1/4, server-computed `security_status`, client에 formula 부재 | tier mapping 높음; formula 비공개라는 negative finding은 중간 |
| SRC-030 | local/execution evidence | `docs/execution/E-002.md:102,116,126,132,151,163,182` | rejected CSS specificity candidate and final 1.1.0 product result | experiment 2026-08-03; reviewed 2026-08-04 | 2026-08-04 | CSS 42→0 후보의 contrast/active 실패와 전량 rollback, final 42 유지 이유 | 높음 |
| SRC-031 | local/isolated build experiment | `origin/main` `e49b5028ad68c55f39eef08405ae78d2971b15ea`; `esbuild.config.mjs`; in-memory esbuild `write:false` comparison of default charset and `charset: "utf8"` | node-platform production graph held constant; 5,268,064 → 5,095,067 bytes (`-172,997`, `-3.28%`) | experiment 2026-08-04 | 2026-08-04 | exact 1.1.2 baseline, UTF-8 distribution-size hypothesis, retained platform/dependency boundary | byte result 높음; live/runtime parity와 scanner 결과는 미검증 |
| SRC-032 | stakeholder decision | 제품 소유자가 승인한 “Achmage Slides Ultra 1.1.3 Scorecard 리팩터링 릴리스 계획”과 2026-08-04 구현 요청 | branch, scope, Dashboard blocker, theme-native slider, low-risk refactor, UTF-8 build와 exclusions 확정 | 2026-08-04 | 2026-08-04 | 1.1.3 제품 결정, acceptance와 release 중단 조건 | 높음(결정) |
| SRC-033 | local/type reproduction | `node_modules/obsidian/obsidian.d.ts`의 `Node.win: Window`, global `createEl/createSpan`; `src/vendor/pretext/measurement.ts`; `src/types/dom-augment.d.ts`; exact TypeScript in-memory compile | lock-installed Obsidian types 1.12.3, minAppVersion 1.7.2 | experiment 2026-08-04 | 2026-08-04 | direct helper TS2339 2건과 type-only Window augmentation 후 0 diagnostics | 높음(current lock); type package 갱신 시 재확인 |

## Revision log

| 날짜 | 변경 내용 | 변경 이유 | 작성자 |
|---|---|---|---|
| 2026-08-02 | R-003 생성 | Community Scorecard Review의 독립 의사결정 범위 등록 | Codex |
| 2026-08-02 | public 225 ledger, local provenance, official rules, build/dependency experiments, phased 권고 및 planning handoff를 추가하고 `complete`로 전환 | 새 UX release에서 아키텍처를 보존하며 Review를 최대한 개선할 근거 확립 | Codex |
| 2026-08-02 | 외부 규칙·로컬 코드 adversarial review 반영: preview/public scan 경계, release SHA 순서, indirect network loads, assertion tranche, test-only fs, provisional bundle protocol, conditional size gate를 교정 | 과장과 내부 모순을 제거하고 계획 인수 조건의 반증 가능성을 높임 | Codex |
| 2026-08-03 | F-015~F-018, REC-015~REC-017 및 AC addendum 추가 | exact dependency/audit graph, canonical artifact와 CRLF 교정, Node 24·draft release·Scorecard blocker, missing native harness 범위를 릴리스 계획에 결정 가능하게 고정 | Codex |
| 2026-08-04 | F-019~F-020, REC-018~REC-019, SC-AC-018~019 추가 | 실제 candidate preview의 비동형 category surface와 complete-before-merge/draft-after-tag 순서를 반영해 1.1.0 gate를 비교 가능하고 실행 가능한 두 단계로 교정 | Codex |
| 2026-08-04 | 공개 `1.1.2` post-release ledger, 512-page tier 표본, F-021~F-025, REC-020~REC-023, SC-AC-020~022, SRC-025~030 추가 | warnings가 77.5% 줄었는데도 Review 막대가 유지된 원인을 categorical tier와 경험적 30/31 경계로 설명하고, CSS 42건을 다음 score-moving 병목으로 분리 | Codex |
| 2026-08-04 | `1.1.3` 결정 부록 F-026~F-028, REC-024~REC-026, SRC-031~SRC-032 추가 | exact `origin/main` 기준에서 theme-native SliderComponent, UTF-8 기능보존형 배포 크기 감소와 declarative settings·Direct FS/browser build 제외 범위를 구현 전에 고정 | Codex |
| 2026-08-04 | F-029, REC-027, SRC-033 추가 | Captured-window Obsidian helper가 current types에서 요구하는 type-only augmentation을 제품 수정 전에 재현·계획화 | Codex |
